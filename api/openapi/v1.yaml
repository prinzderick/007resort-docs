openapi: 3.1.0
info:
  title: 007 Resort & Spa IFOP API
  version: 1.0.0
  summary: Contract of record for backend, mobile, POS, KDS and web clients.
  description: |
    Contract of record for the 007 Resort & Spa platform (Laravel backend, Flutter mobile, Windows POS, KDS, PHP web).

    ## Conventions
    - Base path `/api/v1`; JSON `camelCase`; ids are UUIDv7 strings; timestamps ISO-8601 UTC (`Z`); money is a decimal **string** with currency `NGN`.
    - Every authenticated request carries `Authorization: Bearer <accessToken>` and, from registered devices, `X-Device-Token`.
    - Every mutating request carries `Idempotency-Key`. Mutable aggregates expose `ETag`; updates send `If-Match`.
    - Errors are RFC 7807 `application/problem+json` with a stable `code` (see `ProblemCode`).
    - Lists are cursor paginated: `{ "items": [], "nextCursor": null }`.
    - Permissions are shown as `x-permission` on each operation; clients gate UI on the `permissions` array from login, never on role names.
    - Sensitive actions (void, adjustment, refund, reversal, stock adjustment) return `202` with `ApprovalOutcome` when supervisor approval is required, or accept an `X-Step-Up-Token` for inline supervisor authorisation.
    - Offline creates: `POST /orders`, `POST /orders/{id}/lines`, `POST /tabs` and each tender of `POST /payments` accept an optional client-supplied UUIDv7 `id` (unique per site) plus informational `clientCreatedAt`. Same id and same body replays the original result; same id with a different body is `409 concurrency_conflict`.
  license:
    name: Proprietary
    identifier: LicenseRef-Proprietary
  contact:
    name: 007 Resort engineering
servers:
- url: /api/v1
  description: Same-origin (node-relative)
- url: http://{host}:8080/api/v1
  description: Local node
  variables:
    host:
      default: 192.168.1.10
- url: https://api.007resort.com/api/v1
  description: Cloud node
security:
- bearerAuth: []
  deviceToken: []
tags:
- name: System
  description: Service info and health
- name: Auth
  description: Staff authentication and sessions
- name: Devices
  description: Device registration, tablet checkout, status
- name: Organization
  description: Site, facilities, capabilities
- name: Staff
  description: Staff, roles, role assignments
- name: Catalog
  description: Categories, products, availability
- name: Dining tables
  description: Tables
- name: Orders
  description: Orders and lines
- name: Tabs
  description: Open tabs
- name: Approvals
  description: Supervisor approvals
- name: Payments
  description: Payments, refunds, Paystack
- name: Cash sessions
  description: Cashier drawers
- name: Receipts
  description: Receipt payloads
- name: KDS
  description: Kitchen/bar display
- name: Inventory
  description: Stock
- name: Bookings
  description: Resources and bookings
- name: Entitlements
  description: Tickets, rentals
- name: Memberships
  description: Memberships
- name: Attendance
  description: Biometric attendance
- name: Audit
  description: Audit trail
- name: Reports
  description: Read-only reports
- name: Sync
  description: Local/Cloud sync
- name: Admin
  description: Settings
- name: Realtime
  description: Reverb auth
paths:
  /system/info:
    get:
      operationId: getSystemInfo
      tags:
      - System
      summary: Service info and minimum client versions
      description: Unauthenticated. Clients call this on start-up to check `minClientVersion` and `deploymentMode`.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SystemInfo'
        '429':
          $ref: '#/components/responses/TooMany'
      security: []
  /system/health:
    get:
      operationId: getSystemHealth
      tags:
      - System
      summary: Aggregated health (authenticated detail)
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Health'
        '429':
          $ref: '#/components/responses/TooMany'
      security: []
  /health/live:
    get:
      operationId: getLiveness
      tags:
      - System
      summary: Liveness probe
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Health'
        '429':
          $ref: '#/components/responses/TooMany'
      security: []
      servers:
      - url: /
  /health/ready:
    get:
      operationId: getReadiness
      tags:
      - System
      summary: Readiness probe (DB, Redis, queue)
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Health'
        '429':
          $ref: '#/components/responses/TooMany'
      security: []
      servers:
      - url: /
  /auth/staff/login:
    post:
      operationId: staffLogin
      tags:
      - Auth
      summary: Staff login (password, PIN, or NFC card)
      description: |-
        `X-Device-Token` is required for PIN and NFC_CARD (they are only valid on registered devices) and optional for PASSWORD (admin web). Failed attempts are rate-limited per identifier and device; repeated failures return `account_locked`.

        Problems: `invalid_credentials` (401), `account_locked` (423 mapped as 403), `device_not_registered`/`device_revoked` (403).
      parameters:
      - name: X-Device-Token
        in: header
        required: false
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StaffLoginRequest'
            examples:
              pin:
                summary: PIN on a tablet
                value:
                  credentialType: PIN
                  identifier: S-0042
                  secret: '4821'
              nfc:
                summary: NFC badge
                value:
                  credentialType: NFC_CARD
                  secret: 04A2246B7C5E80
              password:
                summary: Admin web
                value:
                  credentialType: PASSWORD
                  identifier: manager@007resort.com
                  secret: '********'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResult'
              examples:
                ok:
                  value:
                    accessToken: <access-token>
                    refreshToken: <refresh-token>
                    expiresInSeconds: 900
                    staff:
                      id: 0192f6a0-0000-7000-8000-000000000401
                      displayName: Amaka Okafor
                      staffNumber: S-0042
                      roles:
                      - WAIT_STAFF
                      permissions:
                      - order.create
                      - order.line.add
                      - order.line.remove_unsent
                      - order.send
                      - tab.view_own_facility
                      facilityIds:
                      - 0192f6a0-0000-7000-8000-000000000101
                    session:
                      id: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a90
                      expiresAt: '2026-09-23T22:15:30Z'
                      deviceId: 0192f6a0-0000-7000-8000-000000000301
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
        '401':
          $ref: '#/components/responses/Unauthorized'
      security: []
  /auth/staff/refresh:
    post:
      operationId: refreshToken
      tags:
      - Auth
      summary: Rotate the access/refresh token pair
      description: Refresh tokens are single-use and rotate; reuse of a consumed refresh token revokes the whole session (`token_expired`/`unauthenticated`).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefreshRequest'
            examples:
              r:
                value:
                  refreshToken: <refresh-token>
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
      security: []
  /auth/staff/logout:
    post:
      operationId: staffLogout
      tags:
      - Auth
      summary: End the current session
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LogoutRequest'
      responses:
        '204':
          description: Logged out
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /auth/me:
    get:
      operationId: getCurrentStaff
      tags:
      - Auth
      summary: Current staff, permissions and session
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  staff:
                    $ref: '#/components/schemas/Staff'
                  session:
                    $ref: '#/components/schemas/SessionRef'
                  device:
                    oneOf:
                    - $ref: '#/components/schemas/Device'
                    - type: 'null'
                required:
                - staff
                - session
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /auth/staff/step-up:
    post:
      operationId: staffStepUp
      tags:
      - Auth
      summary: Supervisor re-authentication for a sensitive action
      description: Verifies the approver credential (may be a different staff member than the session holder) holds `permission`
        at the device facility scope and returns a single-use token for `X-Step-Up-Token`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StepUpRequest'
            examples:
              sup:
                value:
                  credentialType: PIN
                  identifier: S-0007
                  secret: '9911'
                  permission: order.void.approve
                  entityType: order
                  entityId: 0192f6a0-0000-7000-8000-000000000601
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StepUpResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /auth/sessions/{sessionId}/revoke:
    post:
      operationId: revokeSession
      tags:
      - Auth
      summary: Revoke a session (and optionally lock the device)
      x-permission: session.revoke
      description: 'Required permission: `session.revoke`.'
      parameters:
      - name: sessionId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '204':
          description: Revoked
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /devices/register:
    post:
      operationId: registerDevice
      tags:
      - Devices
      summary: Register a device with a one-time registration code
      description: Registration code is issued by an IT admin (`device.register`). Returns the `deviceToken` exactly once.
      parameters:
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeviceRegisterRequest'
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeviceRegisterResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
      security: []
  /devices:
    get:
      operationId: listDevices
      tags:
      - Devices
      summary: List devices
      x-permission: device.register
      description: 'Required permission: `device.register`.'
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      - name: filter[status]
        in: query
        required: false
        schema:
          type: string
          enum:
          - PENDING
          - ACTIVE
          - REVOKED
      - name: filter[facilityId]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DevicePage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /devices/{deviceId}:
    get:
      operationId: getDevice
      tags:
      - Devices
      summary: Get a device
      parameters:
      - name: deviceId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Device'
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
  /devices/{deviceId}/checkout:
    post:
      operationId: checkoutDevice
      tags:
      - Devices
      summary: Tablet checkout to staff + facility + shift
      description: Binds the device to a staff member, facility and shift. Facility must be within the staff member's role-assignment
        scope. Fails `409 concurrency_conflict` if the device is already checked out.
      parameters:
      - name: deviceId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeviceCheckoutRequest'
            examples:
              c:
                value:
                  staffId: 0192f6a0-0000-7000-8000-000000000401
                  facilityId: 0192f6a0-0000-7000-8000-000000000101
                  shiftId: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a90
                  openingFloat: '0.0000'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Device'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /devices/{deviceId}/checkin:
    post:
      operationId: checkinDevice
      tags:
      - Devices
      summary: Return the tablet (end of shift)
      parameters:
      - name: deviceId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeviceCheckinRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Device'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /devices/{deviceId}/status:
    post:
      operationId: reportDeviceStatus
      tags:
      - Devices
      summary: Device status / heartbeat report
      description: Called every ~60 s while online. Response carries any pending device commands.
      parameters:
      - name: deviceId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeviceStatusReport'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeviceStatusReportAck'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /devices/{deviceId}/revoke:
    post:
      operationId: revokeDevice
      tags:
      - Devices
      summary: Revoke a device
      x-permission: device.revoke
      description: 'Required permission: `device.revoke`.'
      parameters:
      - name: deviceId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Device'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /organization/site:
    get:
      operationId: getSite
      tags:
      - Organization
      summary: The site this node serves
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Site'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /organization/facilities:
    get:
      operationId: listFacilityTree
      tags:
      - Organization
      summary: Facilities as a tree
      description: Not paginated; bounded by site size. Returns only facilities within the caller's scope.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/FacilityNode'
                required:
                - items
              examples:
                t:
                  value:
                    items:
                    - id: 0192f6a0-0000-7000-8000-000000000101
                      siteId: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a90
                      parentId: null
                      code: RESTAURANT_1
                      name: Main Restaurant
                      kind: RESTAURANT
                      status: ACTIVE
                      capabilities:
                      - SELL_FOOD
                      - TABLE_SERVICE
                      rowVersion: 4
                      children: []
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /organization/facilities/{facilityId}:
    get:
      operationId: getFacility
      tags:
      - Organization
      summary: Get a facility
      parameters:
      - name: facilityId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Facility'
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
  /organization/facilities/{facilityId}/operating-points:
    get:
      operationId: listOperatingPoints
      tags:
      - Organization
      summary: Operating points of a facility
      parameters:
      - name: facilityId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperatingPointPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
  /facilities/{facilityId}/capabilities:
    get:
      operationId: getFacilityCapabilities
      tags:
      - Organization
      summary: Effective capabilities and operating rules
      parameters:
      - name: facilityId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FacilityCapabilities'
              examples:
                c:
                  value:
                    facilityId: 0192f6a0-0000-7000-8000-000000000101
                    capabilities:
                    - SELL_FOOD
                    - TABLE_SERVICE
                    - OPEN_TABS
                    operatingRules:
                      approvalThresholdAmount: '5000.0000'
                      requireApprovalFor:
                      - order.void
                      - order.discount
                      allowOpenTabs: true
                      requireCashSession: true
                      allowOfflineOrders: true
                      allowOfflinePayments: CASH_ONLY
                      holdTtlSeconds: 600
                      vatEnabled: false
                      vatRatePercent: '7.5'
                    version: 12
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
  /staff:
    get:
      operationId: listStaff
      tags:
      - Staff
      summary: List staff
      x-permission: staff.manage
      description: 'Required permission: `staff.manage`.'
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      - name: filter[status]
        in: query
        required: false
        schema:
          type: string
      - name: q
        in: query
        required: false
        schema:
          type: string
        description: Name/number search
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StaffMemberPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /staff/{staffId}:
    get:
      operationId: getStaffMember
      tags:
      - Staff
      summary: Get staff member
      x-permission: staff.manage
      description: 'Required permission: `staff.manage`.'
      parameters:
      - name: staffId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StaffMember'
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
  /roles:
    get:
      operationId: listRoles
      tags:
      - Staff
      summary: Roles and their permission bundles
      x-permission: role_assignment.manage
      description: 'Required permission: `role_assignment.manage`.'
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RolePage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /staff/{staffId}/role-assignments:
    get:
      operationId: listRoleAssignments
      tags:
      - Staff
      summary: Role assignments for a staff member
      x-permission: role_assignment.manage
      description: 'Required permission: `role_assignment.manage`.'
      parameters:
      - name: staffId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleAssignmentPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
    post:
      operationId: grantRole
      tags:
      - Staff
      summary: Grant a role at a scope
      x-permission: role_assignment.manage
      description: |-
        Cannot grant a role containing permissions the granter does not hold ("below own level"). Audited.

        Required permission: `role_assignment.manage`.
      parameters:
      - name: staffId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GrantRoleRequest'
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleAssignment'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /staff/{staffId}/role-assignments/{assignmentId}:
    delete:
      operationId: revokeRole
      tags:
      - Staff
      summary: Revoke a role assignment
      x-permission: role_assignment.manage
      description: 'Required permission: `role_assignment.manage`.'
      parameters:
      - name: staffId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: assignmentId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '204':
          description: Revoked
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /catalog/categories:
    get:
      operationId: listCategories
      tags:
      - Catalog
      summary: Product categories
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CategoryPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /catalog/products:
    get:
      operationId: listProducts
      tags:
      - Catalog
      summary: Products for a facility with resolved price/tax/prep route
      description: Clients cache this per facility and refresh with `updatedSince`; ETag supported on the list.
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      - name: facilityId
        in: query
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
        description: Required. Resolves price, tax, availability.
      - name: filter[categoryId]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: filter[kind]
        in: query
        required: false
        schema:
          type: string
      - name: q
        in: query
        required: false
        schema:
          type: string
      - name: updatedSince
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Timestamp'
        description: For incremental cache refresh.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductPage'
              examples:
                p:
                  value:
                    items:
                    - id: 0192f6a0-0000-7000-8000-000000000501
                      sku: FD-JOL-CH
                      name: Jollof Rice & Chicken
                      categoryId: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a90
                      kind: FOOD
                      price: '4500.0000'
                      currency: NGN
                      taxInclusive: true
                      taxRatePercent: '0'
                      taxAmount: '0.0000'
                      prepRoute:
                        stationId: 0192f6a0-0000-7000-8000-000000000901
                        stationName: Kitchen Pass
                        kind: KITCHEN
                      trackStock: true
                      active: true
                      imageUrl: null
                      rowVersion: 3
                    nextCursor: null
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /catalog/products/{productId}:
    get:
      operationId: getProduct
      tags:
      - Catalog
      summary: Get product
      parameters:
      - name: productId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: facilityId
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Product'
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
  /catalog/availability:
    get:
      operationId: listProductAvailability
      tags:
      - Catalog
      summary: Per-facility availability of products
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      - name: facilityId
        in: query
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: filter[productId]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductAvailabilityPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /catalog/products/{productId}/availability/{facilityId}:
    put:
      operationId: setProductAvailability
      tags:
      - Catalog
      summary: Set manual availability (86 an item)
      x-permission: catalog.availability.manage
      description: 'Required permission: `catalog.availability.manage`.'
      parameters:
      - name: productId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: facilityId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetAvailabilityRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductAvailability'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /tables:
    get:
      operationId: listTables
      tags:
      - Dining tables
      summary: List dining tables
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      - name: facilityId
        in: query
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: filter[status]
        in: query
        required: false
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiningTablePage'
              examples:
                t:
                  value:
                    items:
                    - id: 0192f6a0-0000-7000-8000-000000000201
                      facilityId: 0192f6a0-0000-7000-8000-000000000101
                      operatingPointId: null
                      label: T12
                      seats: 4
                      status: FREE
                      openOrderIds: []
                      openTabId: null
                      rowVersion: 1
                    nextCursor: null
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /tables/{tableId}:
    get:
      operationId: getTable
      tags:
      - Dining tables
      summary: Get table with open orders/tab
      parameters:
      - name: tableId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiningTable'
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
    patch:
      operationId: updateTable
      tags:
      - Dining tables
      summary: Set table status
      x-permission: table.manage
      description: 'Required permission: `table.manage`.'
      parameters:
      - name: tableId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      - $ref: '#/components/parameters/IfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTableRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiningTable'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '412':
          description: If-Match precondition failed
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /tables/{tableId}/open:
    post:
      operationId: openTable
      tags:
      - Dining tables
      summary: Mark table occupied (seat guests)
      x-permission: order.create
      description: |-
        Idempotent: opening an already-occupied table by the same session is a no-op returning 200; by a different staff member returns `409 concurrency_conflict`.

        Required permission: `order.create`.
      parameters:
      - name: tableId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiningTable'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /orders:
    post:
      operationId: createOrder
      tags:
      - Orders
      summary: Create a draft order
      x-permission: order.create
      description: |-
        Order number is allocated per facility. If `tableId` is set the table becomes OCCUPIED. `lines` may be supplied to create + add in one call.

        Required permission: `order.create`.
      parameters:
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateOrderRequest'
            examples:
              dinein:
                value:
                  facilityId: 0192f6a0-0000-7000-8000-000000000101
                  tableId: 0192f6a0-0000-7000-8000-000000000201
                  channel: DINE_IN
                  lines:
                  - productId: 0192f6a0-0000-7000-8000-000000000501
                    quantity: 2
                    notes: no pepper
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Order'
              examples:
                created:
                  value:
                    id: 0192f6a0-0000-7000-8000-000000000601
                    number: RST1-000123
                    facilityId: 0192f6a0-0000-7000-8000-000000000101
                    tableId: 0192f6a0-0000-7000-8000-000000000201
                    tabId: null
                    customerName: null
                    channel: DINE_IN
                    status: DRAFT
                    lines:
                    - id: 0192f6a0-0000-7000-8000-000000000701
                      productId: 0192f6a0-0000-7000-8000-000000000501
                      name: Jollof Rice & Chicken
                      quantity: 2
                      unitPrice: '4500.0000'
                      taxAmount: '0.0000'
                      lineTotal: '9000.0000'
                      notes: no pepper
                      status: PENDING
                      prepRoute:
                        stationId: 0192f6a0-0000-7000-8000-000000000901
                        stationName: Kitchen Pass
                        kind: KITCHEN
                      adjustments: []
                    subtotal: '9000.0000'
                    discountTotal: '0.0000'
                    taxTotal: '0.0000'
                    total: '9000.0000'
                    amountPaid: '0.0000'
                    balanceDue: '9000.0000'
                    currency: NGN
                    createdByStaffId: 0192f6a0-0000-7000-8000-000000000401
                    deviceId: 0192f6a0-0000-7000-8000-000000000301
                    pendingApprovalId: null
                    rowVersion: 1
                    createdAt: '2026-09-23T10:15:30.123456Z'
                    updatedAt: '2026-09-23T10:15:30.123456Z'
                    sentAt: null
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
    get:
      operationId: listOrders
      tags:
      - Orders
      summary: List orders
      x-permission: order.view
      description: 'Required permission: `order.view`.'
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      - name: filter[facilityId]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: filter[tableId]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: filter[tabId]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: filter[status]
        in: query
        required: false
        schema:
          type: string
        description: Comma-separated OrderStatus values
      - name: filter[createdByStaffId]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: sort
        in: query
        required: false
        schema:
          type: string
        description: Default `-createdAt`.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrderPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /orders/{orderId}:
    get:
      operationId: getOrder
      tags:
      - Orders
      summary: Get order
      x-permission: order.view
      description: 'Required permission: `order.view`.'
      parameters:
      - name: orderId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Order'
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
  /orders/{orderId}/lines:
    post:
      operationId: addOrderLine
      tags:
      - Orders
      summary: Add a line (DRAFT only)
      x-permission: order.line.add
      description: |-
        Fails `order_state_invalid` (409) unless order is DRAFT, `capability_disabled` if the facility does not sell the product.

        Required permission: `order.line.add`.
      parameters:
      - name: orderId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      - $ref: '#/components/parameters/IfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrderLineInput'
            examples:
              l:
                value:
                  productId: 0192f6a0-0000-7000-8000-000000000501
                  quantity: 1
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Order'
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '412':
          description: If-Match precondition failed
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /orders/{orderId}/lines/{lineId}:
    delete:
      operationId: removeOrderLine
      tags:
      - Orders
      summary: Remove an unsent line
      x-permission: order.line.remove_unsent
      description: 'Required permission: `order.line.remove_unsent`.'
      parameters:
      - name: orderId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: lineId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      - $ref: '#/components/parameters/IfMatch'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Order'
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '412':
          description: If-Match precondition failed
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /orders/{orderId}/send:
    post:
      operationId: sendOrder
      tags:
      - Orders
      summary: Lock lines and route to prep stations
      x-permission: order.send
      description: |-
        In one transaction: locks lines, reserves/consumes stock per facility rule, creates PrepTickets per station, and emits `prep-ticket.created` and `order.updated`. Fails `insufficient_stock` (409) with `meta.itemId`.

        Required permission: `order.send`.
      parameters:
      - name: orderId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      - $ref: '#/components/parameters/IfMatch'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendOrderRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Order'
              examples:
                sent:
                  value:
                    id: 0192f6a0-0000-7000-8000-000000000601
                    number: RST1-000123
                    facilityId: 0192f6a0-0000-7000-8000-000000000101
                    tableId: 0192f6a0-0000-7000-8000-000000000201
                    tabId: null
                    customerName: null
                    channel: DINE_IN
                    status: SENT
                    lines:
                    - id: 0192f6a0-0000-7000-8000-000000000701
                      productId: 0192f6a0-0000-7000-8000-000000000501
                      name: Jollof Rice & Chicken
                      quantity: 2
                      unitPrice: '4500.0000'
                      taxAmount: '0.0000'
                      lineTotal: '9000.0000'
                      notes: no pepper
                      status: ROUTED
                      prepRoute:
                        stationId: 0192f6a0-0000-7000-8000-000000000901
                        stationName: Kitchen Pass
                        kind: KITCHEN
                      adjustments: []
                    subtotal: '9000.0000'
                    discountTotal: '0.0000'
                    taxTotal: '0.0000'
                    total: '9000.0000'
                    amountPaid: '0.0000'
                    balanceDue: '9000.0000'
                    currency: NGN
                    createdByStaffId: 0192f6a0-0000-7000-8000-000000000401
                    deviceId: 0192f6a0-0000-7000-8000-000000000301
                    pendingApprovalId: null
                    rowVersion: 3
                    createdAt: '2026-09-23T10:15:30.123456Z'
                    updatedAt: '2026-09-23T10:15:30.123456Z'
                    sentAt: '2026-09-23T10:15:30.123456Z'
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '412':
          description: If-Match precondition failed
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /orders/{orderId}/serve:
    post:
      operationId: serveOrder
      tags:
      - Orders
      summary: Mark order served (all READY lines DISPENSED)
      x-permission: order.serve
      description: 'Required permission: `order.serve`.'
      parameters:
      - name: orderId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      - $ref: '#/components/parameters/IfMatch'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Order'
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '412':
          description: If-Match precondition failed
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /orders/{orderId}/void:
    post:
      operationId: voidOrder
      tags:
      - Orders
      summary: Void an order (sensitive)
      x-permission: order.void.execute
      description: |-
        Behaviour: (1) caller holds `order.void.approve` or supplies a valid `X-Step-Up-Token` -> `200` and the order is VOIDED. (2) approval required by facility rule -> `202` with `ApprovalOutcome`; the order becomes `PENDING_APPROVAL` and a supervisor decides via `POST /approvals/{id}/decision`. (3) Caller lacks `order.void.execute` -> `403 permission_denied`.

        Required permission: `order.void.execute`.
      parameters:
      - name: orderId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      - $ref: '#/components/parameters/IfMatch'
      - $ref: '#/components/parameters/StepUpToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VoidRequest'
            examples:
              v:
                value:
                  reason: Customer left before food arrived
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Order'
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '412':
          description: If-Match precondition failed
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
        '202':
          description: Approval requested
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalOutcome'
              examples:
                pending:
                  value:
                    status: PENDING_APPROVAL
                    approval:
                      id: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a91
                      action: order.void
                      entityType: order
                      entityId: 0192f6a0-0000-7000-8000-000000000601
                      facilityId: 0192f6a0-0000-7000-8000-000000000101
                      status: PENDING
                      requestedByStaffId: 0192f6a0-0000-7000-8000-000000000401
                      requestedByName: Amaka O.
                      requestedAt: '2026-09-23T10:15:30.123456Z'
                      reason: Customer left
                      amount: '9000.0000'
                      summary: Void order RST1-000123 (9,000.00)
                      decidedByStaffId: null
                      decidedAt: null
                      decisionNote: null
                      requiredPermission: order.void.approve
      x-approval-gated: true
  /orders/{orderId}/lines/{lineId}/adjustments:
    post:
      operationId: adjustOrderLine
      tags:
      - Orders
      summary: Discount / price override / comp on a line (sensitive)
      x-permission: order.discount.execute
      description: |-
        Same 200/202/403 semantics as void. Permission by kind: `order.discount.execute`, `order.price_override.execute`, `order.comp.execute`.

        Required permission: `order.discount.execute`.
      parameters:
      - name: orderId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: lineId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      - $ref: '#/components/parameters/IfMatch'
      - $ref: '#/components/parameters/StepUpToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdjustmentRequest'
            examples:
              d:
                value:
                  kind: DISCOUNT_PERCENT
                  value: '10'
                  reason: Loyal guest
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Order'
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '412':
          description: If-Match precondition failed
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
        '202':
          description: Approval requested
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalOutcome'
      x-approval-gated: true
  /tabs:
    post:
      operationId: openTab
      tags:
      - Tabs
      summary: Open a tab
      x-permission: tab.open
      description: 'Required permission: `tab.open`.'
      parameters:
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OpenTabRequest'
            examples:
              t:
                value:
                  facilityId: 0192f6a0-0000-7000-8000-000000000101
                  tableId: 0192f6a0-0000-7000-8000-000000000201
                  customerName: Mr Bello
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Tab'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
    get:
      operationId: listTabs
      tags:
      - Tabs
      summary: List tabs
      x-permission: tab.view_own_facility
      description: 'Required permission: `tab.view_own_facility`.'
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      - name: filter[facilityId]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: filter[status]
        in: query
        required: false
        schema:
          type: string
      - name: filter[tableId]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TabPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /tabs/{tabId}:
    get:
      operationId: getTab
      tags:
      - Tabs
      summary: Get tab
      x-permission: tab.view_own_facility
      description: 'Required permission: `tab.view_own_facility`.'
      parameters:
      - name: tabId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Tab'
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
  /tabs/{tabId}/orders:
    post:
      operationId: addOrdersToTab
      tags:
      - Tabs
      summary: Attach orders to a tab
      x-permission: tab.open
      description: 'Required permission: `tab.open`.'
      parameters:
      - name: tabId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      - $ref: '#/components/parameters/IfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddTabOrdersRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Tab'
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '412':
          description: If-Match precondition failed
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /tabs/{tabId}/settle:
    post:
      operationId: settleTab
      tags:
      - Tabs
      summary: Settle a tab with one or more tenders
      x-permission: order.settle
      description: |-
        Equivalent to `POST /payments` allocating to every order on the tab, under a tab row lock. Second concurrent settle returns `409 balance_changed`. Requires an OPEN cash session if any tender is CASH and facility `requireCashSession`.

        Required permission: `order.settle`.
      parameters:
      - name: tabId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SettleTabRequest'
            examples:
              s:
                value:
                  tenders:
                  - tenderType: CASH
                    amount: '9000.0000'
                    tendered: '10000.0000'
                  cashSessionId: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a92
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentResult'
              examples:
                ok:
                  value:
                    payments:
                    - id: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a90
                      groupId: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a91
                      facilityId: 0192f6a0-0000-7000-8000-000000000101
                      tenderType: CASH
                      provider: MANUAL
                      providerReference: null
                      reference: null
                      status: CAPTURED
                      amount: '9000.0000'
                      tendered: '10000.0000'
                      changeGiven: '1000.0000'
                      refundedAmount: '0.0000'
                      currency: NGN
                      allocations:
                      - orderId: 0192f6a0-0000-7000-8000-000000000601
                        amount: '9000.0000'
                      cashSessionId: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a92
                      receiptId: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a91
                      takenByStaffId: 0192f6a0-0000-7000-8000-000000000401
                      createdAt: '2026-09-23T10:15:30.123456Z'
                      capturedAt: '2026-09-23T10:15:30.123456Z'
                    orders: []
                    receiptId: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a91
                    changeDue: '1000.0000'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /approvals:
    get:
      operationId: listApprovals
      tags:
      - Approvals
      summary: List approvals (pending for the caller by default)
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      - name: filter[status]
        in: query
        required: false
        schema:
          type: string
        description: Default PENDING
      - name: filter[facilityId]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: scope
        in: query
        required: false
        schema:
          type: string
          enum:
          - mine
          - approvable
        description: '`approvable` = ones the caller can decide.'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalPage'
              examples:
                a:
                  value:
                    items:
                    - id: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a91
                      action: order.void
                      entityType: order
                      entityId: 0192f6a0-0000-7000-8000-000000000601
                      facilityId: 0192f6a0-0000-7000-8000-000000000101
                      status: PENDING
                      requestedByStaffId: 0192f6a0-0000-7000-8000-000000000401
                      requestedByName: Amaka O.
                      requestedAt: '2026-09-23T10:15:30.123456Z'
                      reason: Customer left
                      amount: '9000.0000'
                      summary: Void order RST1-000123 (9,000.00)
                      decidedByStaffId: null
                      decidedAt: null
                      decisionNote: null
                      requiredPermission: order.void.approve
                    nextCursor: null
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /approvals/{approvalId}:
    get:
      operationId: getApproval
      tags:
      - Approvals
      summary: Get approval
      parameters:
      - name: approvalId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Approval'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
  /approvals/{approvalId}/decision:
    post:
      operationId: decideApproval
      tags:
      - Approvals
      summary: Approve or reject
      description: Caller must hold the approval's `requiredPermission`, and must not be the requester (`permission_denied`
        otherwise). Applying the approved action and audit happen in the same transaction. Emits `approval.decided` to the
        requesting device.
      parameters:
      - name: approvalId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApprovalDecisionRequest'
            examples:
              ok:
                value:
                  decision: APPROVE
                  note: ok
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Approval'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /approvals/{approvalId}/cancel:
    post:
      operationId: cancelApproval
      tags:
      - Approvals
      summary: Requester cancels a pending approval
      parameters:
      - name: approvalId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Approval'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /payments:
    post:
      operationId: createPayment
      tags:
      - Payments
      summary: Create payment(s) with allocations and tenders
      x-permission: payment.take
      description: |-
        Atomic. Locks target orders (ordered by id), validates balance, writes one immutable Payment per tender + allocations + receipt, transitions fully paid orders to SETTLED, and emits `order.updated`. Replays with same `Idempotency-Key` return the original response. Problems: `balance_changed`, `amount_mismatch`, `cash_session_required`, `order_state_invalid`.

        Required permission: `payment.take`.
      parameters:
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePaymentRequest'
            examples:
              cash:
                summary: Cash with change
                value:
                  facilityId: 0192f6a0-0000-7000-8000-000000000101
                  cashSessionId: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a92
                  allocations:
                  - orderId: 0192f6a0-0000-7000-8000-000000000601
                    amount: '9000.0000'
                  tenders:
                  - tenderType: CASH
                    amount: '9000.0000'
                    tendered: '10000.0000'
              split:
                summary: Split cash + POS
                value:
                  facilityId: 0192f6a0-0000-7000-8000-000000000101
                  cashSessionId: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a92
                  allocations:
                  - orderId: 0192f6a0-0000-7000-8000-000000000601
                    amount: '9000.0000'
                  tenders:
                  - tenderType: CASH
                    amount: '4000.0000'
                    tendered: '4000.0000'
                  - tenderType: POS_TERMINAL
                    amount: '5000.0000'
                    reference: RRN123456789
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentResult'
              examples:
                ok:
                  value:
                    payments:
                    - id: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a90
                      groupId: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a91
                      facilityId: 0192f6a0-0000-7000-8000-000000000101
                      tenderType: CASH
                      provider: MANUAL
                      providerReference: null
                      reference: null
                      status: CAPTURED
                      amount: '9000.0000'
                      tendered: '10000.0000'
                      changeGiven: '1000.0000'
                      refundedAmount: '0.0000'
                      currency: NGN
                      allocations:
                      - orderId: 0192f6a0-0000-7000-8000-000000000601
                        amount: '9000.0000'
                      cashSessionId: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a92
                      receiptId: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a91
                      takenByStaffId: 0192f6a0-0000-7000-8000-000000000401
                      createdAt: '2026-09-23T10:15:30.123456Z'
                      capturedAt: '2026-09-23T10:15:30.123456Z'
                    orders: []
                    receiptId: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a91
                    changeDue: '1000.0000'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
    get:
      operationId: listPayments
      tags:
      - Payments
      summary: List payments
      x-permission: payment.view
      description: 'Required permission: `payment.view`.'
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      - name: filter[facilityId]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: filter[orderId]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: filter[status]
        in: query
        required: false
        schema:
          type: string
      - name: filter[cashSessionId]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: filter[from]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Timestamp'
      - name: filter[to]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Timestamp'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /payments/{paymentId}:
    get:
      operationId: getPayment
      tags:
      - Payments
      summary: Get payment
      x-permission: payment.view
      description: 'Required permission: `payment.view`.'
      parameters:
      - name: paymentId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Payment'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
  /payments/{paymentId}/refund:
    post:
      operationId: refundPayment
      tags:
      - Payments
      summary: Refund (partial or full)
      x-permission: refund.execute
      description: |-
        Creates an immutable Refund record; payment moves to PARTIALLY_REFUNDED/REFUNDED. May return `202`-style `PENDING_APPROVAL` refund body (status field) when approval is required; `approval_required` (403) if the caller cannot even request.

        Required permission: `refund.execute`.
      parameters:
      - name: paymentId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      - $ref: '#/components/parameters/StepUpToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefundRequest'
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Refund'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
      x-approval-gated: true
  /payments/{paymentId}/reversal:
    post:
      operationId: reversePayment
      tags:
      - Payments
      summary: Reverse a payment (same-session correction)
      x-permission: payment.reversal.execute
      description: 'Required permission: `payment.reversal.execute`.'
      parameters:
      - name: paymentId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      - $ref: '#/components/parameters/StepUpToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReversalRequest'
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentReversal'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
      x-approval-gated: true
  /payments/paystack/initialize:
    post:
      operationId: paystackInitialize
      tags:
      - Payments
      summary: Initialise a Paystack transaction
      x-permission: payment.take
      description: |-
        Creates a Payment in AUTHORIZING. Amount must equal the server-computed balance (`amount_mismatch` otherwise). The customer completes payment at `authorizationUrl`.

        Required permission: `payment.take`.
      parameters:
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PaystackInitRequest'
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaystackInitResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /payments/paystack/verify/{reference}:
    get:
      operationId: paystackVerify
      tags:
      - Payments
      summary: Verify a Paystack transaction by reference
      description: Server calls Paystack; safe to poll. Capture is idempotent with the webhook.
      parameters:
      - name: reference
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Payment'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
  /payments/webhooks/{provider}:
    post:
      operationId: paymentWebhook
      tags:
      - Payments
      summary: Provider webhook inbox (signature-verified)
      description: '`provider` = `paystack`. Deduplicated on provider event id (`provider_event` UNIQUE); duplicates return
        200 with `duplicate: true`. Never requires bearer auth. Cloud node is the public receiver; Local receives via sync
        (`OnlinePaymentConfirmed`).'
      parameters:
      - name: provider
        in: path
        required: true
        schema:
          type: string
          enum:
          - paystack
      - name: X-Paystack-Signature
        in: header
        required: false
        schema:
          type: string
        description: HMAC-SHA512 of raw body. Invalid -> 401 with no processing.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProviderEventAck'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
      security: []
  /cash-sessions:
    post:
      operationId: openCashSession
      tags:
      - Cash sessions
      summary: Open a cash session on this device
      x-permission: cash_session.open
      description: 'Required permission: `cash_session.open`.'
      parameters:
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OpenCashSessionRequest'
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CashSession'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
    get:
      operationId: listCashSessions
      tags:
      - Cash sessions
      summary: List cash sessions
      x-permission: cash_session.view
      description: 'Required permission: `cash_session.view`.'
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      - name: filter[facilityId]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: filter[status]
        in: query
        required: false
        schema:
          type: string
      - name: filter[staffId]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CashSessionPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /cash-sessions/{cashSessionId}:
    get:
      operationId: getCashSession
      tags:
      - Cash sessions
      summary: Get cash session
      parameters:
      - name: cashSessionId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CashSession'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
  /cash-sessions/{cashSessionId}/close:
    post:
      operationId: closeCashSession
      tags:
      - Cash sessions
      summary: Close with counted cash
      x-permission: cash_session.close
      description: 'Required permission: `cash_session.close`.'
      parameters:
      - name: cashSessionId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CloseCashSessionRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CashSession'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /receipts/{receiptId}:
    get:
      operationId: getReceipt
      tags:
      - Receipts
      summary: Receipt payload for printing
      x-permission: receipt.view
      description: 'Required permission: `receipt.view`.'
      parameters:
      - name: receiptId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: reprint
        in: query
        required: false
        schema:
          type: boolean
        description: If true, increments reprintCount (needs `receipt.reprint`) and marks the copy as DUPLICATE.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Receipt'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
  /orders/{orderId}/receipt:
    get:
      operationId: getOrderReceipt
      tags:
      - Receipts
      summary: Latest receipt for an order
      x-permission: receipt.view
      description: 'Required permission: `receipt.view`.'
      parameters:
      - name: orderId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Receipt'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
  /kds/stations:
    get:
      operationId: listKdsStations
      tags:
      - KDS
      summary: Stations for a facility
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      - name: facilityId
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KdsStationPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /kds/stations/{stationId}/tickets:
    get:
      operationId: listStationTickets
      tags:
      - KDS
      summary: Current board for a station
      x-permission: prep_ticket.view
      description: |-
        Also pushed live on `private-kds.station.{stationId}`. Always reload this after a websocket reconnect.

        Required permission: `prep_ticket.view`.
      parameters:
      - name: stationId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      - name: filter[status]
        in: query
        required: false
        schema:
          type: string
        description: Comma-separated PrepTicketStatus. Default NEW,ACCEPTED,IN_PROGRESS,READY.
      - name: updatedSince
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Timestamp'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PrepTicketPage'
              examples:
                b:
                  value:
                    items:
                    - id: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a92
                      number: K-042
                      stationId: 0192f6a0-0000-7000-8000-000000000901
                      orderId: 0192f6a0-0000-7000-8000-000000000601
                      orderNumber: RST1-000123
                      facilityId: 0192f6a0-0000-7000-8000-000000000101
                      tableLabel: T12
                      status: NEW
                      items:
                      - orderLineId: 0192f6a0-0000-7000-8000-000000000701
                        name: Jollof Rice & Chicken
                        quantity: 2
                        notes: no pepper
                        status: NEW
                      createdAt: '2026-09-23T10:15:30.123456Z'
                      acceptedAt: null
                      readyAt: null
                      waiterStaffId: 0192f6a0-0000-7000-8000-000000000401
                      waiterName: Amaka O.
                      rowVersion: 1
                    nextCursor: null
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
  /prep-tickets/{ticketId}:
    get:
      operationId: getPrepTicket
      tags:
      - KDS
      summary: Get prep ticket
      x-permission: prep_ticket.view
      description: 'Required permission: `prep_ticket.view`.'
      parameters:
      - name: ticketId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PrepTicket'
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
  /prep-tickets/{ticketId}/transition:
    post:
      operationId: transitionPrepTicket
      tags:
      - KDS
      summary: Advance a prep ticket
      x-permission: prep_ticket.transition
      description: |-
        Allowed: NEW->ACCEPTED->IN_PROGRESS->READY->DISPENSED (NEW->IN_PROGRESS and ACCEPTED->READY allowed to skip). Other transitions: `order_state_invalid`. READY on the last ticket moves the order to READY and emits `order.ready`.

        Required permission: `prep_ticket.transition`.
      parameters:
      - name: ticketId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      - $ref: '#/components/parameters/IfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransitionRequest'
            examples:
              accept:
                value:
                  to: ACCEPTED
              ready:
                value:
                  to: READY
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PrepTicket'
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '412':
          description: If-Match precondition failed
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /inventory/items:
    get:
      operationId: listInventoryItems
      tags:
      - Inventory
      summary: Inventory items
      x-permission: inventory.view
      description: 'Required permission: `inventory.view`.'
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      - name: q
        in: query
        required: false
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InventoryItemPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /inventory/locations:
    get:
      operationId: listStockLocations
      tags:
      - Inventory
      summary: Stock locations
      x-permission: inventory.view
      description: 'Required permission: `inventory.view`.'
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockLocationPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /inventory/balances:
    get:
      operationId: listStockBalances
      tags:
      - Inventory
      summary: Current balances
      x-permission: inventory.view
      description: 'Required permission: `inventory.view`.'
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      - name: locationId
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: itemId
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockBalancePage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /inventory/purchase-receipts:
    post:
      operationId: createPurchaseReceipt
      tags:
      - Inventory
      summary: Receive stock into a store
      x-permission: inventory.purchase_receipt.create
      description: |-
        Movements are immutable; corrections are new movements. Negative stock is refused with `insufficient_stock` unless the facility rule allows it.

        Required permission: `inventory.purchase_receipt.create`.
      parameters:
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PurchaseReceiptRequest'
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockMovement'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /inventory/transfers:
    post:
      operationId: createTransfer
      tags:
      - Inventory
      summary: Transfer stock between locations
      x-permission: inventory.transfer.create
      description: |-
        Movements are immutable; corrections are new movements. Negative stock is refused with `insufficient_stock` unless the facility rule allows it.

        Required permission: `inventory.transfer.create`.
      parameters:
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransferRequest'
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockMovement'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /inventory/adjustments:
    post:
      operationId: createAdjustment
      tags:
      - Inventory
      summary: Stock adjustment (sensitive; may be PENDING_APPROVAL)
      x-permission: inventory.adjustment.request
      description: |-
        Movements are immutable; corrections are new movements. Negative stock is refused with `insufficient_stock` unless the facility rule allows it.

        Required permission: `inventory.adjustment.request`.
      parameters:
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdjustmentInvRequest'
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockMovement'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /inventory/wastage:
    post:
      operationId: createWastage
      tags:
      - Inventory
      summary: Record wastage
      x-permission: inventory.wastage.create
      description: |-
        Movements are immutable; corrections are new movements. Negative stock is refused with `insufficient_stock` unless the facility rule allows it.

        Required permission: `inventory.wastage.create`.
      parameters:
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WastageRequest'
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockMovement'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /inventory/counts:
    post:
      operationId: createStockCount
      tags:
      - Inventory
      summary: Create a count sheet with counted quantities
      x-permission: inventory.count.create
      description: 'Required permission: `inventory.count.create`.'
      parameters:
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CountRequest'
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockCount'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /inventory/counts/{countId}/post:
    post:
      operationId: postStockCount
      tags:
      - Inventory
      summary: 'Post count: writes variance movements'
      x-permission: inventory.count.post
      description: |-
        Variance above threshold creates a PENDING_APPROVAL adjustment.

        Required permission: `inventory.count.post`.
      parameters:
      - name: countId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockCount'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /bookings/resources:
    get:
      operationId: listBookableResources
      tags:
      - Bookings
      summary: Bookable resources
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      - name: facilityId
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BookableResourcePage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /bookings/resources/{resourceId}/availability:
    get:
      operationId: getResourceAvailability
      tags:
      - Bookings
      summary: Slot availability window
      parameters:
      - name: resourceId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: from
        in: query
        required: true
        schema:
          $ref: '#/components/schemas/Timestamp'
      - name: to
        in: query
        required: true
        schema:
          $ref: '#/components/schemas/Timestamp'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Availability'
              examples:
                a:
                  value:
                    resourceId: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a90
                    slots:
                    - start: '2026-09-24T09:00:00Z'
                      end: '2026-09-24T10:00:00Z'
                      available: true
                      remainingCapacity: 1
                      price: '5000.0000'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
  /bookings/hold:
    post:
      operationId: holdBooking
      tags:
      - Bookings
      summary: Place a short-TTL hold on a slot
      x-permission: booking.create
      description: |-
        Backed by a UNIQUE slot allocation; a concurrent hold loses with `409 slot_unavailable`. Hold TTL from facility rule (default 600 s); expiry emits release. Confirm before `holdExpiresAt` or get `hold_expired`.

        Required permission: `booking.create`.
      parameters:
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HoldRequest'
            examples:
              h:
                value:
                  resourceId: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a90
                  start: '2026-09-24T09:00:00Z'
                  end: '2026-09-24T10:00:00Z'
                  quantity: 1
                  customer:
                    name: Chinedu Eze
                    phone: '+2348012345678'
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Booking'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /bookings:
    get:
      operationId: listBookings
      tags:
      - Bookings
      summary: List bookings
      x-permission: booking.view
      description: 'Required permission: `booking.view`.'
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      - name: filter[resourceId]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: filter[facilityId]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: filter[status]
        in: query
        required: false
        schema:
          type: string
      - name: filter[from]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Timestamp'
      - name: filter[to]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Timestamp'
      - name: q
        in: query
        required: false
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BookingPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /bookings/{bookingId}:
    get:
      operationId: getBooking
      tags:
      - Bookings
      summary: Get booking
      x-permission: booking.view
      description: 'Required permission: `booking.view`.'
      parameters:
      - name: bookingId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Booking'
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
  /bookings/{bookingId}/confirm:
    post:
      operationId: confirmBooking
      tags:
      - Bookings
      summary: Confirm booking (payment) and issue entitlement
      x-permission: booking.create
      description: 'Required permission: `booking.create`.'
      parameters:
      - name: bookingId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      - $ref: '#/components/parameters/IfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfirmBookingRequest'
            examples:
              cash:
                value:
                  tenders:
                  - tenderType: CASH
                    amount: '5000.0000'
                    tendered: '5000.0000'
                  cashSessionId: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a92
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Booking'
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '412':
          description: If-Match precondition failed
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /bookings/{bookingId}/cancel:
    post:
      operationId: cancelBooking
      tags:
      - Bookings
      summary: Cancel per policy
      x-permission: booking.cancel
      description: 'Required permission: `booking.cancel`.'
      parameters:
      - name: bookingId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      - $ref: '#/components/parameters/IfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CancelBookingRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Booking'
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '412':
          description: If-Match precondition failed
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /bookings/{bookingId}/reschedule:
    post:
      operationId: rescheduleBooking
      tags:
      - Bookings
      summary: Reschedule to a new slot
      x-permission: booking.reschedule
      description: |-
        New slot is held before the old one is released, in one transaction.

        Required permission: `booking.reschedule`.
      parameters:
      - name: bookingId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      - $ref: '#/components/parameters/IfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RescheduleBookingRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Booking'
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '412':
          description: If-Match precondition failed
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /entitlements:
    post:
      operationId: issueEntitlement
      tags:
      - Entitlements
      summary: Issue entitlement for an order or booking
      x-permission: ticket.issue
      description: |-
        Normally issued automatically on payment/confirm; this endpoint is for re-issue or manual issue. Idempotent per source.

        Required permission: `ticket.issue`.
      parameters:
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IssueEntitlementRequest'
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Entitlement'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /entitlements/{entitlementId}:
    get:
      operationId: getEntitlement
      tags:
      - Entitlements
      summary: Get entitlement by id
      x-permission: ticket.view
      description: 'Required permission: `ticket.view`.'
      parameters:
      - name: entitlementId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Entitlement'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
  /entitlement-tokens/{qrToken}:
    get:
      operationId: getEntitlementByToken
      tags:
      - Entitlements
      summary: 'Lookup by QR token (Sports Store: what was paid/rented)'
      x-permission: ticket.view
      description: 'Required permission: `ticket.view`.'
      parameters:
      - name: qrToken
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Entitlement'
              examples:
                e:
                  value:
                    id: 0192f6a0-0000-7000-8000-000000000801
                    qrToken: <qr-token>
                    status: ACTIVE
                    orderId: 0192f6a0-0000-7000-8000-000000000601
                    bookingId: null
                    holderName: Chinedu Eze
                    items:
                    - id: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a90
                      kind: ACCESS
                      name: Tennis Court 1 - 09:00
                      facilityId: 0192f6a0-0000-7000-8000-000000000101
                      quantity: 1
                      quantityRedeemed: 0
                      validationMode: SINGLE_USE
                      validFrom: '2026-09-24T08:45:00Z'
                      validUntil: '2026-09-24T10:00:00Z'
                      rentalStatus: null
                    - id: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a91
                      kind: RENTAL
                      name: Racket x2
                      facilityId: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a92
                      quantity: 2
                      quantityRedeemed: 0
                      validationMode: NONE
                      validFrom: null
                      validUntil: null
                      rentalStatus: NOT_RELEASED
                    issuedAt: '2026-09-23T10:15:30.123456Z'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
  /entitlement-tokens/{qrToken}/redeem:
    post:
      operationId: redeemEntitlement
      tags:
      - Entitlements
      summary: Redeem ENTRY at a facility (scan)
      x-permission: ticket.redeem
      description: |-
        Atomic conditional update (`qty_redeemed < qty`) — of two concurrent scans exactly one gets VALID. Result is in the body; HTTP is 200 for every scan outcome. Facility is checked against the device checkout facility (or `facilityId`). Offline scanners must not fabricate results; see README.

        Required permission: `ticket.redeem`.
      parameters:
      - name: qrToken
        in: path
        required: true
        schema:
          type: string
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RedeemRequest'
            examples:
              entry:
                value:
                  action: ENTRY
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RedeemResult'
              examples:
                valid:
                  value:
                    result: VALID
                    entitlementId: 0192f6a0-0000-7000-8000-000000000801
                    itemId: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a90
                    holderName: Chinedu Eze
                    itemName: Tennis Court 1 - 09:00
                    remaining: 0
                    redemptionId: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a92
                    message: Welcome
                used:
                  value:
                    result: USED
                    entitlementId: 0192f6a0-0000-7000-8000-000000000801
                    itemId: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a90
                    holderName: Chinedu Eze
                    itemName: Tennis Court 1 - 09:00
                    remaining: 0
                    redemptionId: null
                    message: Already used at 09:02
                    usedAt: '2026-09-24T09:02:11Z'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /entitlement-tokens/{qrToken}/exit:
    post:
      operationId: exitEntitlement
      tags:
      - Entitlements
      summary: Record EXIT for ENTRY_EXIT tickets (pool)
      x-permission: ticket.redeem
      description: 'Required permission: `ticket.redeem`.'
      parameters:
      - name: qrToken
        in: path
        required: true
        schema:
          type: string
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExitRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RedeemResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /entitlements/{entitlementId}/release:
    post:
      operationId: releaseRental
      tags:
      - Entitlements
      summary: Release rental items to the customer (Sports Store)
      x-permission: ticket.release
      description: 'Required permission: `ticket.release`.'
      parameters:
      - name: entitlementId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReleaseRequest'
            examples:
              r:
                value:
                  itemIds:
                  - 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a91
                  note: 2 rackets + balls
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Entitlement'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /entitlements/{entitlementId}/return:
    post:
      operationId: returnRental
      tags:
      - Entitlements
      summary: Take rental items back (Sports Store)
      x-permission: ticket.release
      description: 'Required permission: `ticket.release`.'
      parameters:
      - name: entitlementId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReturnRequest'
            examples:
              r:
                value:
                  itemIds:
                  - 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a91
                  condition: OK
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Entitlement'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /memberships/plans:
    get:
      operationId: listMembershipPlans
      tags:
      - Memberships
      summary: Membership plans
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MembershipPlanPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /memberships:
    post:
      operationId: purchaseMembership
      tags:
      - Memberships
      summary: Purchase a membership
      x-permission: membership.sell
      description: 'Required permission: `membership.sell`.'
      parameters:
      - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PurchaseMembershipRequest'
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Membership'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
    get:
      operationId: listMemberships
      tags:
      - Memberships
      summary: List memberships
      x-permission: membership.view
      description: 'Required permission: `membership.view`.'
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      - name: q
        in: query
        required: false
        schema:
          type: string
      - name: filter[status]
        in: query
        required: false
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MembershipPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /memberships/{membershipId}:
    get:
      operationId: getMembership
      tags:
      - Memberships
      summary: Membership status
      x-permission: membership.view
      description: 'Required permission: `membership.view`.'
      parameters:
      - name: membershipId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Membership'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
  /memberships/validate:
    post:
      operationId: validateMembership
      tags:
      - Memberships
      summary: Validate a membership QR at a facility
      x-permission: membership.validate
      description: 'Required permission: `membership.validate`.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                qrToken:
                  type: string
                facilityId:
                  $ref: '#/components/schemas/Uuid'
              required:
              - qrToken
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MembershipValidation'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /attendance/punches:
    post:
      operationId: ingestPunches
      tags:
      - Attendance
      summary: Ingest biometric punches (ZKTeco push)
      description: Authenticated with `X-Device-Token` of an ATTENDANCE_TERMINAL device only (no Bearer). Dedup on `(terminalSerial,
        rawId)`; replays are safe and counted as `duplicates`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PunchIngest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PunchIngestResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
      security:
      - deviceToken: []
  /attendance:
    get:
      operationId: listAttendance
      tags:
      - Attendance
      summary: Staff attendance list
      x-permission: attendance.view
      description: 'Required permission: `attendance.view`.'
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      - name: filter[staffId]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: filter[from]
        in: query
        required: false
        schema:
          type: string
          format: date
      - name: filter[to]
        in: query
        required: false
        schema:
          type: string
          format: date
      - name: filter[status]
        in: query
        required: false
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttendanceRecordPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /audit:
    get:
      operationId: queryAudit
      tags:
      - Audit
      summary: Query audit trail
      x-permission: audit.view
      description: 'Required permission: `audit.view`.'
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      - name: entityType
        in: query
        required: false
        schema:
          type: string
      - name: entityId
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: action
        in: query
        required: false
        schema:
          type: string
      - name: actorStaffId
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: filter[from]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Timestamp'
      - name: filter[to]
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Timestamp'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditEntryPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /reports/facility-daily-summary:
    get:
      operationId: facilityDailySummary
      tags:
      - Reports
      summary: Facility daily summary
      x-permission: report.view
      description: 'Required permission: `report.view`.'
      parameters:
      - name: date
        in: query
        required: true
        schema:
          type: string
          format: date
      - name: facilityId
        in: query
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FacilityDailySummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /reports/cashier-shift/{shiftId}:
    get:
      operationId: cashierShiftReport
      tags:
      - Reports
      summary: Cashier shift reconciliation
      x-permission: report.view
      description: 'Required permission: `report.view`.'
      parameters:
      - name: shiftId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CashierShiftReport'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
  /sync/inbox:
    post:
      operationId: syncInbox
      tags:
      - Sync
      summary: Receive sync events from the peer node
      description: Node-to-node only; authenticated with a node credential (`bearerAuth` with node token, no device). Dedup
        by `eventId`; per-event `result`. Exactly-once *effect*, at-least-once delivery.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SyncInboxRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SyncInboxResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /sync/pull:
    get:
      operationId: syncPull
      tags:
      - Sync
      summary: Pull events/commands queued by the peer
      description: 'Local polls Cloud (ADR-0005: Local never accepts inbound).'
      parameters:
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Cursor'
      - name: since
        in: query
        required: false
        schema:
          type: string
        description: Opaque cursor from previous pull
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SyncEventPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /sync/heartbeat:
    post:
      operationId: syncHeartbeat
      tags:
      - Sync
      summary: Node heartbeat
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SyncHeartbeatRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SyncHeartbeatResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /sync/status:
    get:
      operationId: syncStatus
      tags:
      - Sync
      summary: Sync/outbox status for dashboards
      x-permission: config.manage
      description: 'Required permission: `config.manage`.'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SyncStatus'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
  /admin/settings/tax:
    get:
      operationId: getTaxSetting
      tags:
      - Admin
      summary: Get VAT/tax setting
      x-permission: config.manage
      description: 'Required permission: `config.manage`.'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaxSetting'
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooMany'
    put:
      operationId: updateTaxSetting
      tags:
      - Admin
      summary: Update VAT/tax setting (default off, ADR-0011)
      x-permission: config.manage
      description: |-
        Owner/admin only. Changes apply to new orders; existing orders keep their computed tax. Audited; emits `ConfigurationUpdated` sync event.

        Required permission: `config.manage`.
      parameters:
      - $ref: '#/components/parameters/IdempotencyKey'
      - $ref: '#/components/parameters/IfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTaxSettingRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaxSetting'
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '412':
          description: If-Match precondition failed
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /broadcasting/auth:
    post:
      operationId: broadcastAuth
      tags:
      - Realtime
      summary: Authorise a private Reverb/Pusher channel subscription
      description: Pusher protocol private-channel auth. See `api/realtime.md`. `403 permission_denied` when the caller may
        not subscribe to the channel.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BroadcastAuthRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BroadcastAuthResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/Unprocessable'
        '429':
          $ref: '#/components/responses/TooMany'
  /devices/{deviceId}/commands:
    get:
      operationId: listDeviceCommands
      tags:
      - Devices
      summary: Pending device commands (poll fallback)
      description: Fallback for `device.command` when websocket is down.
      parameters:
      - name: deviceId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/DeviceCommand'
                required:
                - items
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooMany'
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: opaque or JWT
      description: Staff session access token from `/auth/staff/login`.
    deviceToken:
      type: apiKey
      in: header
      name: X-Device-Token
      description: Registered device secret from `/devices/register`.
  parameters:
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      schema:
        type: string
        minLength: 16
        maxLength: 128
      description: 'Client-generated (UUID recommended), stable across retries of the same logical operation. Replay with
        same key+same body returns the original response (with `Idempotent-Replayed: true`); same key with different body
        -> `422 idempotency_key_reused`. Keys live 72 h.'
    IfMatch:
      name: If-Match
      in: header
      required: true
      schema:
        type: string
      description: ETag from the last GET/response of the aggregate. Mismatch -> `412`/`409 concurrency_conflict`; missing
        -> `428`.
    StepUpToken:
      name: X-Step-Up-Token
      in: header
      required: false
      schema:
        type: string
      description: Single-use token from `POST /auth/staff/step-up`, letting a supervisor authorise the action inline.
    Limit:
      name: limit
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 200
        default: 50
    Cursor:
      name: cursor
      in: query
      schema:
        type: string
      description: '`nextCursor` from previous page.'
  headers:
    ETag:
      description: Entity tag derived from the aggregate `rowVersion`.
      schema:
        type: string
      example: '"v3"'
  responses:
    BadRequest:
      description: Malformed request or missing Idempotency-Key
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
          example:
            type: https://api.007resort.com/problems/idempotency_key_missing
            title: Malformed request or missing Idempotency-Key
            status: 400
            code: idempotency_key_missing
            detail: Malformed request or missing Idempotency-Key
            correlationId: c1f4e7a2-9b8d-4c33-a1de-6f0a2b7c9d10
    Unauthorized:
      description: Missing, expired or invalid credentials
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
          example:
            type: https://api.007resort.com/problems/unauthenticated
            title: Missing, expired or invalid credentials
            status: 401
            code: unauthenticated
            detail: Missing, expired or invalid credentials
            correlationId: c1f4e7a2-9b8d-4c33-a1de-6f0a2b7c9d10
    Forbidden:
      description: Permission denied or approval required
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
          example:
            type: https://api.007resort.com/problems/permission_denied
            title: Permission denied or approval required
            status: 403
            code: permission_denied
            detail: Permission denied or approval required
            correlationId: c1f4e7a2-9b8d-4c33-a1de-6f0a2b7c9d10
    NotFound:
      description: Resource not found
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
          example:
            type: https://api.007resort.com/problems/not_found
            title: Resource not found
            status: 404
            code: not_found
            detail: Resource not found
            correlationId: c1f4e7a2-9b8d-4c33-a1de-6f0a2b7c9d10
    Conflict:
      description: State or concurrency conflict (concurrency_conflict, insufficient_stock, slot_unavailable, balance_changed,
        order_state_invalid ...)
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
          example:
            type: https://api.007resort.com/problems/concurrency_conflict
            title: State or concurrency conflict (concurrency_conflict, insufficient_stock, slot_unavailable, balance_changed,
              order_state_invalid ...)
            status: 409
            code: concurrency_conflict
            detail: State or concurrency conflict (concurrency_conflict, insufficient_stock, slot_unavailable, balance_changed,
              order_state_invalid ...)
            correlationId: c1f4e7a2-9b8d-4c33-a1de-6f0a2b7c9d10
    Unprocessable:
      description: Validation failed
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
          example:
            type: https://api.007resort.com/problems/validation_failed
            title: Validation failed
            status: 422
            code: validation_failed
            detail: Validation failed
            correlationId: c1f4e7a2-9b8d-4c33-a1de-6f0a2b7c9d10
    TooMany:
      description: Rate limited; honour Retry-After
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
          example:
            type: https://api.007resort.com/problems/rate_limited
            title: Rate limited; honour Retry-After
            status: 429
            code: rate_limited
            detail: Rate limited; honour Retry-After
            correlationId: c1f4e7a2-9b8d-4c33-a1de-6f0a2b7c9d10
  schemas:
    Uuid:
      type: string
      format: uuid
      description: UUIDv7 in canonical lowercase form.
      example: 0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a90
    Money:
      type: string
      pattern: ^-?\d+(\.\d{1,4})?$
      description: Decimal string, up to 4 fractional digits. Never a JSON number.
      example: '1500.0000'
    Timestamp:
      type: string
      format: date-time
      description: ISO-8601 UTC with `Z` suffix.
      example: '2026-09-23T10:15:30.123456Z'
    Problem:
      type: object
      description: RFC 7807 problem details with a stable machine-readable `code`.
      required:
      - type
      - title
      - status
      - code
      properties:
        type:
          type: string
          format: uri-reference
          example: https://api.007resort.com/problems/insufficient_stock
        title:
          type: string
          example: Insufficient stock
        status:
          type: integer
          example: 409
        detail:
          type: string
        instance:
          type: string
        code:
          $ref: '#/components/schemas/ProblemCode'
        errors:
          type: object
          description: Field-level validation errors keyed by JSON pointer-ish field path (camelCase).
          additionalProperties:
            type: array
            items:
              type: string
          example:
            lines.0.quantity:
            - must be at least 1
        approvalId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        correlationId:
          type: string
        meta:
          type: object
          additionalProperties: true
          description: Code-specific extra data (e.g. `currentRowVersion`, `availableQuantity`).
    ProblemCode:
      type: string
      enum:
      - unauthenticated
      - token_expired
      - invalid_credentials
      - account_locked
      - device_not_registered
      - device_revoked
      - permission_denied
      - approval_required
      - approval_pending
      - step_up_required
      - validation_failed
      - not_found
      - concurrency_conflict
      - idempotency_key_missing
      - idempotency_key_reused
      - insufficient_stock
      - slot_unavailable
      - hold_expired
      - ticket_used
      - ticket_invalid
      - order_state_invalid
      - payment_state_invalid
      - amount_mismatch
      - balance_changed
      - cash_session_required
      - capability_disabled
      - facility_mismatch
      - offline_not_allowed
      - rate_limited
      - provider_error
      - server_error
      description: Stable, additive enum. Clients must treat unknown values as a generic error of the HTTP status class.
    SystemInfo:
      type: object
      properties:
        service:
          type: string
          example: 007resort-api
        apiVersion:
          type: string
          example: 1.0.0
        deploymentMode:
          type: string
          enum:
          - local
          - cloud
        nodeId:
          $ref: '#/components/schemas/Uuid'
        serverTime:
          $ref: '#/components/schemas/Timestamp'
        timezone:
          type: string
          example: Africa/Lagos
        currency:
          type: string
          enum:
          - NGN
        minClientVersion:
          type: object
          additionalProperties:
            type: string
          example:
            mobile: 0.1.0
            pos: 0.1.0
            kds: 0.1.0
        vatEnabled:
          type: boolean
        realtime:
          type: object
          properties:
            scheme:
              type: string
              enum:
              - ws
              - wss
            host:
              type: string
            port:
              type: integer
            appKey:
              type: string
              description: Public Reverb app key.
          required:
          - scheme
          - host
          - port
          - appKey
      required:
      - service
      - apiVersion
      - deploymentMode
      - serverTime
      - currency
    Health:
      type: object
      properties:
        status:
          type: string
          enum:
          - ok
          - degraded
          - down
        checks:
          type: object
          additionalProperties:
            type: string
            enum:
            - ok
            - degraded
            - down
          example:
            database: ok
            redis: ok
            queue: ok
            cloudLink: degraded
        serverTime:
          $ref: '#/components/schemas/Timestamp'
      required:
      - status
    StaffLoginRequest:
      type: object
      properties:
        credentialType:
          type: string
          enum:
          - PASSWORD
          - PIN
          - NFC_CARD
        identifier:
          type: string
          description: Staff username or staff number (PASSWORD, PIN). Omitted for NFC_CARD.
        secret:
          type: string
          description: Password, PIN, or NFC card UID/hash depending on credentialType.
          writeOnly: true
      required:
      - credentialType
      - secret
    Staff:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        displayName:
          type: string
        staffNumber:
          type: string
        roles:
          type: array
          items:
            type: string
        permissions:
          type: array
          items:
            type: string
            example: order.create
        facilityIds:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
      required:
      - id
      - displayName
      - roles
      - permissions
      description: '`roles` are informational labels; clients MUST gate UI on `permissions`, never role names.'
    SessionRef:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        expiresAt:
          $ref: '#/components/schemas/Timestamp'
        deviceId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
      required:
      - id
    AuthResult:
      type: object
      properties:
        accessToken:
          type: string
        refreshToken:
          type: string
        expiresInSeconds:
          type: integer
          example: 900
        staff:
          $ref: '#/components/schemas/Staff'
        session:
          $ref: '#/components/schemas/SessionRef'
      required:
      - accessToken
      - refreshToken
      - expiresInSeconds
      - staff
      - session
    RefreshRequest:
      type: object
      properties:
        refreshToken:
          type: string
      required:
      - refreshToken
    LogoutRequest:
      type: object
      properties:
        allSessions:
          type: boolean
    StepUpRequest:
      type: object
      properties:
        credentialType:
          type: string
          enum:
          - PASSWORD
          - PIN
          - NFC_CARD
        identifier:
          type: string
        secret:
          type: string
          writeOnly: true
        permission:
          type: string
          description: Permission the token will authorise, e.g. `order.void.approve`.
        entityType:
          type: string
        entityId:
          $ref: '#/components/schemas/Uuid'
      required:
      - credentialType
      - secret
      - permission
    StepUpResult:
      type: object
      properties:
        stepUpToken:
          type: string
          description: Single-use, short-lived. Send as `X-Step-Up-Token` on the sensitive request.
        expiresInSeconds:
          type: integer
          example: 120
        approver:
          type: object
          properties:
            id:
              $ref: '#/components/schemas/Uuid'
            displayName:
              type: string
          required:
          - id
          - displayName
      required:
      - stepUpToken
      - expiresInSeconds
      - approver
    DeviceKind:
      type: string
      enum:
      - MOBILE_TABLET
      - POS_TERMINAL
      - KDS_SCREEN
      - ENTRANCE_SCANNER
      - ATTENDANCE_TERMINAL
      - SERVER
    Device:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        kind:
          $ref: '#/components/schemas/DeviceKind'
        status:
          type: string
          enum:
          - PENDING
          - ACTIVE
          - REVOKED
        facilityId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        homeFacilityId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        platform:
          type: string
          example: android
        appVersion:
          type: string
        lastSeenAt:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
        checkout:
          oneOf:
          - $ref: '#/components/schemas/DeviceCheckout'
          - type: 'null'
        rowVersion:
          type: integer
      required:
      - id
      - name
      - kind
      - status
    DeviceRegisterRequest:
      type: object
      properties:
        name:
          type: string
          example: Tablet 04
        kind:
          $ref: '#/components/schemas/DeviceKind'
        hardwareId:
          type: string
          description: Stable platform identifier (Android ID / install UUID).
        platform:
          type: string
        appVersion:
          type: string
        registrationCode:
          type: string
          description: One-time code issued by IT admin in the admin UI.
          writeOnly: true
      required:
      - name
      - kind
      - hardwareId
      - registrationCode
    DeviceRegisterResult:
      type: object
      properties:
        device:
          $ref: '#/components/schemas/Device'
        deviceToken:
          type: string
          description: Long-lived opaque secret. Store in secure storage; send as `X-Device-Token`. Shown once.
      required:
      - device
      - deviceToken
    DeviceCheckout:
      type: object
      properties:
        staffId:
          $ref: '#/components/schemas/Uuid'
        facilityId:
          $ref: '#/components/schemas/Uuid'
        shiftId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        checkedOutAt:
          $ref: '#/components/schemas/Timestamp'
        checkedInAt:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
      required:
      - staffId
      - facilityId
      - checkedOutAt
    DeviceCheckoutRequest:
      type: object
      properties:
        staffId:
          $ref: '#/components/schemas/Uuid'
        facilityId:
          $ref: '#/components/schemas/Uuid'
        shiftId:
          $ref: '#/components/schemas/Uuid'
        openingFloat:
          $ref: '#/components/schemas/Money'
      required:
      - staffId
      - facilityId
    DeviceCheckinRequest:
      type: object
      properties:
        closingNote:
          type: string
        cashSessionId:
          $ref: '#/components/schemas/Uuid'
    DeviceStatusReport:
      type: object
      properties:
        appVersion:
          type: string
        batteryPercent:
          type: integer
          minimum: 0
          maximum: 100
        networkType:
          type: string
          enum:
          - WIFI
          - CELLULAR
          - ETHERNET
          - NONE
        queuedMutations:
          type: integer
          description: Offline queue depth.
        oldestQueuedAt:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
        lastSyncedAt:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
        printerStatus:
          type: string
          enum:
          - OK
          - OFFLINE
          - NO_PAPER
          - UNKNOWN
      required:
      - appVersion
    DeviceCommand:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        command:
          type: string
          enum:
          - FORCE_LOGOUT
          - REFRESH_STATE
          - LOCK
          - REVOKE
        issuedAt:
          $ref: '#/components/schemas/Timestamp'
        payload:
          type: object
          additionalProperties: true
      required:
      - id
      - command
      - issuedAt
      - payload
    Site:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        organizationId:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
          example: 007 Resort & Spa
        timezone:
          type: string
          example: Africa/Lagos
        currency:
          type: string
          enum:
          - NGN
        address:
          type: string
      required:
      - id
      - name
      - timezone
      - currency
    Facility:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        siteId:
          $ref: '#/components/schemas/Uuid'
        parentId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        code:
          type: string
          example: RESTAURANT_1
        name:
          type: string
        kind:
          type: string
          description: Facility category, e.g. RESTAURANT, BAR, SPORTS_STORE, SPORTS_ENTRANCE, POOL, RECEPTION, MAIN_STORE.
          example: RESTAURANT
        status:
          type: string
          enum:
          - ACTIVE
          - INACTIVE
        capabilities:
          type: array
          items:
            type: string
        rowVersion:
          type: integer
      required:
      - id
      - siteId
      - code
      - name
      - kind
      - status
    FacilityNode:
      allOf:
      - $ref: '#/components/schemas/Facility'
      - type: object
        properties:
          children:
            type: array
            items:
              $ref: '#/components/schemas/FacilityNode'
        required:
        - children
    OperatingPoint:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        facilityId:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
          example: BAR_COUNTER_1
        name:
          type: string
        kind:
          type: string
          enum:
          - TABLE_AREA
          - COUNTER
          - GATE
          - STORE_WINDOW
          - STATION
          - ROOM
        defaultPrepStationId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
      required:
      - id
      - facilityId
      - code
      - name
      - kind
    OperatingRules:
      type: object
      properties:
        approvalThresholdAmount:
          $ref: '#/components/schemas/Money'
        requireApprovalFor:
          type: array
          items:
            type: string
            example: order.void
        allowOpenTabs:
          type: boolean
        requireCashSession:
          type: boolean
        allowOfflineOrders:
          type: boolean
        allowOfflinePayments:
          type: string
          enum:
          - NONE
          - CASH_ONLY
          - ALL
        holdTtlSeconds:
          type: integer
        vatEnabled:
          type: boolean
        vatRatePercent:
          type: string
          example: '7.5'
    FacilityCapabilities:
      type: object
      properties:
        facilityId:
          $ref: '#/components/schemas/Uuid'
        capabilities:
          type: array
          items:
            type: string
            example: SELL_FOOD
        operatingRules:
          $ref: '#/components/schemas/OperatingRules'
        version:
          type: integer
      required:
      - facilityId
      - capabilities
      - operatingRules
    StaffMember:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        displayName:
          type: string
        staffNumber:
          type: string
        status:
          type: string
          enum:
          - ACTIVE
          - SUSPENDED
          - TERMINATED
        email:
          type:
          - string
          - 'null'
          format: email
        phone:
          type:
          - string
          - 'null'
        rowVersion:
          type: integer
      required:
      - id
      - displayName
      - status
    Role:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
          example: CASHIER
        name:
          type: string
        permissions:
          type: array
          items:
            type: string
      required:
      - id
      - code
      - name
      - permissions
    RoleAssignment:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        staffId:
          $ref: '#/components/schemas/Uuid'
        roleId:
          $ref: '#/components/schemas/Uuid'
        scopeType:
          type: string
          enum:
          - ORGANIZATION
          - SITE
          - FACILITY
        scopeId:
          $ref: '#/components/schemas/Uuid'
        grantedByStaffId:
          $ref: '#/components/schemas/Uuid'
        grantedAt:
          $ref: '#/components/schemas/Timestamp'
        revokedAt:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
      required:
      - id
      - staffId
      - roleId
      - scopeType
      - scopeId
    GrantRoleRequest:
      type: object
      properties:
        roleId:
          $ref: '#/components/schemas/Uuid'
        scopeType:
          type: string
          enum:
          - ORGANIZATION
          - SITE
          - FACILITY
        scopeId:
          $ref: '#/components/schemas/Uuid'
      required:
      - roleId
      - scopeType
      - scopeId
    Category:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        parentId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        name:
          type: string
        sortOrder:
          type: integer
      required:
      - id
      - name
    PrepRoute:
      type: object
      properties:
        stationId:
          $ref: '#/components/schemas/Uuid'
        stationName:
          type: string
        kind:
          type: string
          enum:
          - KITCHEN
          - BAR
          - NONE
      required:
      - kind
    Product:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        sku:
          type: string
        name:
          type: string
        categoryId:
          $ref: '#/components/schemas/Uuid'
        kind:
          type: string
          enum:
          - FOOD
          - DRINK
          - RETAIL
          - TICKET
          - RENTAL
          - MEMBERSHIP
          - SERVICE
        price:
          $ref: '#/components/schemas/Money'
        currency:
          type: string
          enum:
          - NGN
        taxInclusive:
          type: boolean
        taxRatePercent:
          type: string
          example: '7.5'
        taxAmount:
          $ref: '#/components/schemas/Money'
        prepRoute:
          $ref: '#/components/schemas/PrepRoute'
        trackStock:
          type: boolean
        active:
          type: boolean
        imageUrl:
          type:
          - string
          - 'null'
          format: uri
        rowVersion:
          type: integer
      required:
      - id
      - name
      - categoryId
      - kind
      - price
      - currency
      - prepRoute
      - active
      description: '`price`, `taxAmount` are resolved for the requested facility. When VAT is disabled `taxRatePercent` is
        `"0"`.'
    ProductAvailability:
      type: object
      properties:
        productId:
          $ref: '#/components/schemas/Uuid'
        facilityId:
          $ref: '#/components/schemas/Uuid'
        available:
          type: boolean
        reason:
          type:
          - string
          - 'null'
          enum:
          - OUT_OF_STOCK
          - NOT_SOLD_HERE
          - INACTIVE
          - MANUALLY_DISABLED
          - null
        quantityOnHand:
          type:
          - string
          - 'null'
          description: Decimal string.
      required:
      - productId
      - facilityId
      - available
    SetAvailabilityRequest:
      type: object
      properties:
        available:
          type: boolean
        reason:
          type: string
      required:
      - available
    DiningTable:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        facilityId:
          $ref: '#/components/schemas/Uuid'
        operatingPointId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        label:
          type: string
          example: T12
        seats:
          type: integer
        status:
          type: string
          enum:
          - FREE
          - OCCUPIED
          - RESERVED
          - NEEDS_CLEANING
        openOrderIds:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
        openTabId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        rowVersion:
          type: integer
      required:
      - id
      - facilityId
      - label
      - status
    UpdateTableRequest:
      type: object
      properties:
        status:
          type: string
          enum:
          - FREE
          - OCCUPIED
          - RESERVED
          - NEEDS_CLEANING
      required:
      - status
    OrderStatus:
      type: string
      enum:
      - DRAFT
      - SENT
      - IN_PREPARATION
      - READY
      - SERVED
      - SETTLED
      - VOIDED
      - PENDING_APPROVAL
    OrderLineStatus:
      type: string
      enum:
      - PENDING
      - LOCKED
      - ROUTED
      - ACCEPTED
      - IN_PROGRESS
      - READY
      - DISPENSED
      - VOIDED
      - REMOVED
    OrderLine:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        productId:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        quantity:
          type: integer
          minimum: 1
        unitPrice:
          $ref: '#/components/schemas/Money'
        taxAmount:
          $ref: '#/components/schemas/Money'
        lineTotal:
          $ref: '#/components/schemas/Money'
        notes:
          type:
          - string
          - 'null'
        status:
          $ref: '#/components/schemas/OrderLineStatus'
        prepRoute:
          $ref: '#/components/schemas/PrepRoute'
        adjustments:
          type: array
          items:
            $ref: '#/components/schemas/LineAdjustment'
      required:
      - id
      - productId
      - name
      - quantity
      - unitPrice
      - lineTotal
      - status
    LineAdjustment:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        kind:
          type: string
          enum:
          - DISCOUNT_PERCENT
          - DISCOUNT_AMOUNT
          - PRICE_OVERRIDE
          - COMP
        value:
          type: string
          description: Percent (e.g. `"10"`) or money decimal, per kind.
        reason:
          type: string
        amount:
          $ref: '#/components/schemas/Money'
        status:
          type: string
          enum:
          - PENDING_APPROVAL
          - APPLIED
          - REJECTED
        approvalId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
      required:
      - id
      - kind
      - value
      - reason
      - status
    Order:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        number:
          type: string
          example: RST1-000123
        facilityId:
          $ref: '#/components/schemas/Uuid'
        tableId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        tabId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        customerName:
          type:
          - string
          - 'null'
        channel:
          type: string
          enum:
          - DINE_IN
          - TAKEAWAY
          - ROOM
          - COUNTER
          - ONLINE
        status:
          $ref: '#/components/schemas/OrderStatus'
        lines:
          type: array
          items:
            $ref: '#/components/schemas/OrderLine'
        subtotal:
          $ref: '#/components/schemas/Money'
        discountTotal:
          $ref: '#/components/schemas/Money'
        taxTotal:
          $ref: '#/components/schemas/Money'
        total:
          $ref: '#/components/schemas/Money'
        amountPaid:
          $ref: '#/components/schemas/Money'
        balanceDue:
          $ref: '#/components/schemas/Money'
        currency:
          type: string
          enum:
          - NGN
        createdByStaffId:
          $ref: '#/components/schemas/Uuid'
        deviceId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        pendingApprovalId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        rowVersion:
          type: integer
        createdAt:
          $ref: '#/components/schemas/Timestamp'
        updatedAt:
          $ref: '#/components/schemas/Timestamp'
        sentAt:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
      required:
      - id
      - number
      - facilityId
      - status
      - lines
      - subtotal
      - taxTotal
      - total
      - amountPaid
      - balanceDue
      - currency
      - rowVersion
      - createdAt
    OrderSummary:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        number:
          type: string
        facilityId:
          $ref: '#/components/schemas/Uuid'
        tableId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        tableLabel:
          type:
          - string
          - 'null'
        tabId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        status:
          $ref: '#/components/schemas/OrderStatus'
        total:
          $ref: '#/components/schemas/Money'
        balanceDue:
          $ref: '#/components/schemas/Money'
        lineCount:
          type: integer
        createdAt:
          $ref: '#/components/schemas/Timestamp'
      required:
      - id
      - number
      - facilityId
      - status
      - total
      - balanceDue
    OrderLineInput:
      type: object
      properties:
        productId:
          $ref: '#/components/schemas/Uuid'
        quantity:
          type: integer
          minimum: 1
          maximum: 999
        notes:
          type: string
          maxLength: 255
        id:
          type: string
          format: uuid
          description: OPTIONAL client-supplied UUIDv7, scoped per site. Server uses it if valid and unused. Replay with same
            id and same body returns the original resource (no duplicate); same id with a different body returns 409 concurrency_conflict;
            a non-UUIDv7 value returns 422 validation_failed. For a line this becomes the order line id.
        clientCreatedAt:
          type: string
          format: date-time
          description: OPTIONAL informational client clock (ISO-8601 UTC). Server keeps its own createdAt.
      required:
      - productId
      - quantity
    CreateOrderRequest:
      type: object
      properties:
        facilityId:
          $ref: '#/components/schemas/Uuid'
        tableId:
          $ref: '#/components/schemas/Uuid'
        tabId:
          $ref: '#/components/schemas/Uuid'
        channel:
          type: string
          enum:
          - DINE_IN
          - TAKEAWAY
          - ROOM
          - COUNTER
        customerName:
          type: string
        lines:
          type: array
          items:
            $ref: '#/components/schemas/OrderLineInput'
        id:
          type: string
          format: uuid
          description: OPTIONAL client-supplied UUIDv7, scoped per site. Server uses it if valid and unused. Replay with same
            id and same body returns the original resource (no duplicate); same id with a different body returns 409 concurrency_conflict;
            a non-UUIDv7 value returns 422 validation_failed.
        clientCreatedAt:
          type: string
          format: date-time
          description: OPTIONAL informational client clock (ISO-8601 UTC). Server keeps its own createdAt.
      required:
      - facilityId
    SendOrderRequest:
      type: object
      properties:
        lineIds:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
      description: Optional. Omit to send all pending lines.
    VoidRequest:
      type: object
      properties:
        reason:
          type: string
          minLength: 3
      required:
      - reason
    AdjustmentRequest:
      type: object
      properties:
        kind:
          type: string
          enum:
          - DISCOUNT_PERCENT
          - DISCOUNT_AMOUNT
          - PRICE_OVERRIDE
          - COMP
        value:
          type: string
        reason:
          type: string
          minLength: 3
      required:
      - kind
      - value
      - reason
    ApprovalOutcome:
      type: object
      properties:
        status:
          type: string
          enum:
          - PENDING_APPROVAL
        approval:
          $ref: '#/components/schemas/Approval'
        order:
          $ref: '#/components/schemas/Order'
      required:
      - status
      - approval
      description: Returned with HTTP 202 when the action was accepted but is gated on supervisor approval. Nothing was applied
        yet.
    Tab:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        facilityId:
          $ref: '#/components/schemas/Uuid'
        tableId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        customerName:
          type: string
        status:
          type: string
          enum:
          - OPEN
          - SETTLING
          - SETTLED
          - VOIDED
        orderIds:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
        total:
          $ref: '#/components/schemas/Money'
        amountPaid:
          $ref: '#/components/schemas/Money'
        balanceDue:
          $ref: '#/components/schemas/Money'
        currency:
          type: string
          enum:
          - NGN
        openedByStaffId:
          $ref: '#/components/schemas/Uuid'
        openedAt:
          $ref: '#/components/schemas/Timestamp'
        rowVersion:
          type: integer
      required:
      - id
      - facilityId
      - status
      - orderIds
      - total
      - amountPaid
      - balanceDue
      - openedAt
    OpenTabRequest:
      type: object
      properties:
        facilityId:
          $ref: '#/components/schemas/Uuid'
        tableId:
          $ref: '#/components/schemas/Uuid'
        customerName:
          type: string
        orderIds:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
        id:
          type: string
          format: uuid
          description: OPTIONAL client-supplied UUIDv7, scoped per site. Server uses it if valid and unused. Replay with same
            id and same body returns the original resource (no duplicate); same id with a different body returns 409 concurrency_conflict;
            a non-UUIDv7 value returns 422 validation_failed.
        clientCreatedAt:
          type: string
          format: date-time
          description: OPTIONAL informational client clock (ISO-8601 UTC). Server keeps its own createdAt.
      required:
      - facilityId
    AddTabOrdersRequest:
      type: object
      properties:
        orderIds:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
      required:
      - orderIds
    SettleTabRequest:
      type: object
      properties:
        tenders:
          type: array
          items:
            $ref: '#/components/schemas/TenderInput'
        cashSessionId:
          $ref: '#/components/schemas/Uuid'
      required:
      - tenders
      description: Settles every unsettled order on the tab in one atomic payment. Sum of tenders must equal or exceed balance
        (cash change is returned in the payment).
    Approval:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        action:
          type: string
          example: order.void
        entityType:
          type: string
        entityId:
          $ref: '#/components/schemas/Uuid'
        facilityId:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
          enum:
          - PENDING
          - APPROVED
          - REJECTED
          - EXPIRED
          - CANCELLED
        requestedByStaffId:
          $ref: '#/components/schemas/Uuid'
        requestedByName:
          type: string
        requestedAt:
          $ref: '#/components/schemas/Timestamp'
        reason:
          type: string
        amount:
          oneOf:
          - $ref: '#/components/schemas/Money'
          - type: 'null'
        summary:
          type: string
          example: Void order RST1-000123 (12,500.00)
        decidedByStaffId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        decidedAt:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
        decisionNote:
          type:
          - string
          - 'null'
        requiredPermission:
          type: string
          example: order.void.approve
      required:
      - id
      - action
      - entityType
      - entityId
      - status
      - requestedAt
      - reason
    ApprovalDecisionRequest:
      type: object
      properties:
        decision:
          type: string
          enum:
          - APPROVE
          - REJECT
        note:
          type: string
        stepUpToken:
          type: string
          description: Required if the deciding supervisor is not the logged-in session holder.
      required:
      - decision
    TenderType:
      type: string
      enum:
      - CASH
      - CARD
      - TRANSFER
      - POS_TERMINAL
    TenderInput:
      type: object
      properties:
        tenderType:
          $ref: '#/components/schemas/TenderType'
        amount:
          $ref: '#/components/schemas/Money'
        reference:
          type: string
          description: POS terminal RRN / transfer reference / card slip no.
        tendered:
          type: string
          description: Cash handed over (CASH only); change = tendered - amount.
        id:
          type: string
          format: uuid
          description: OPTIONAL client-supplied UUIDv7, scoped per site. Server uses it if valid and unused. Replay with same
            id and same body returns the original resource (no duplicate); same id with a different body returns 409 concurrency_conflict;
            a non-UUIDv7 value returns 422 validation_failed. For a tender this becomes the Payment id.
        clientCreatedAt:
          type: string
          format: date-time
          description: OPTIONAL informational client clock (ISO-8601 UTC). Server keeps its own createdAt.
      required:
      - tenderType
      - amount
    AllocationInput:
      type: object
      properties:
        orderId:
          $ref: '#/components/schemas/Uuid'
        amount:
          $ref: '#/components/schemas/Money'
      required:
      - orderId
      - amount
    CreatePaymentRequest:
      type: object
      properties:
        facilityId:
          $ref: '#/components/schemas/Uuid'
        cashSessionId:
          $ref: '#/components/schemas/Uuid'
        allocations:
          type: array
          items:
            $ref: '#/components/schemas/AllocationInput'
        tabId:
          $ref: '#/components/schemas/Uuid'
        tenders:
          type: array
          items:
            $ref: '#/components/schemas/TenderInput'
        customerName:
          type: string
        clientCreatedAt:
          type: string
          format: date-time
          description: OPTIONAL informational client clock (ISO-8601 UTC). Server keeps its own createdAt.
      required:
      - facilityId
      - allocations
      - tenders
      description: '`allocations` distribute the payment across one or more orders. Sum of allocations must equal sum of non-cash-change
        tenders. One `Payment` record is created per tender (split payments); they share `groupId`.'
    PaymentAllocation:
      type: object
      properties:
        orderId:
          $ref: '#/components/schemas/Uuid'
        amount:
          $ref: '#/components/schemas/Money'
      required:
      - orderId
      - amount
    Payment:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        groupId:
          $ref: '#/components/schemas/Uuid'
        facilityId:
          $ref: '#/components/schemas/Uuid'
        tenderType:
          $ref: '#/components/schemas/TenderType'
        provider:
          type:
          - string
          - 'null'
          enum:
          - PAYSTACK
          - MANUAL
          - null
        providerReference:
          type:
          - string
          - 'null'
        reference:
          type:
          - string
          - 'null'
        status:
          type: string
          enum:
          - INITIATED
          - AUTHORIZING
          - CAPTURED
          - FAILED
          - CANCELLED
          - PARTIALLY_REFUNDED
          - REFUNDED
          - REVERSED
        amount:
          $ref: '#/components/schemas/Money'
        tendered:
          oneOf:
          - $ref: '#/components/schemas/Money'
          - type: 'null'
        changeGiven:
          oneOf:
          - $ref: '#/components/schemas/Money'
          - type: 'null'
        refundedAmount:
          $ref: '#/components/schemas/Money'
        currency:
          type: string
          enum:
          - NGN
        allocations:
          type: array
          items:
            $ref: '#/components/schemas/PaymentAllocation'
        cashSessionId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        receiptId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        takenByStaffId:
          $ref: '#/components/schemas/Uuid'
        createdAt:
          $ref: '#/components/schemas/Timestamp'
        capturedAt:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
      required:
      - id
      - facilityId
      - tenderType
      - status
      - amount
      - currency
      - allocations
      - createdAt
    PaymentResult:
      type: object
      properties:
        payments:
          type: array
          items:
            $ref: '#/components/schemas/Payment'
        orders:
          type: array
          items:
            $ref: '#/components/schemas/OrderSummary'
        receiptId:
          $ref: '#/components/schemas/Uuid'
        changeDue:
          $ref: '#/components/schemas/Money'
      required:
      - payments
      - receiptId
    RefundRequest:
      type: object
      properties:
        amount:
          $ref: '#/components/schemas/Money'
        reason:
          type: string
          minLength: 3
        tenderType:
          $ref: '#/components/schemas/TenderType'
      required:
      - amount
      - reason
    Refund:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        paymentId:
          $ref: '#/components/schemas/Uuid'
        amount:
          $ref: '#/components/schemas/Money'
        reason:
          type: string
        status:
          type: string
          enum:
          - PENDING_APPROVAL
          - COMPLETED
          - FAILED
        approvalId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        createdAt:
          $ref: '#/components/schemas/Timestamp'
      required:
      - id
      - paymentId
      - amount
      - status
    ReversalRequest:
      type: object
      properties:
        reason:
          type: string
          minLength: 3
      required:
      - reason
    PaymentReversal:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        paymentId:
          $ref: '#/components/schemas/Uuid'
        reason:
          type: string
        status:
          type: string
          enum:
          - PENDING_APPROVAL
          - COMPLETED
        approvalId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        createdAt:
          $ref: '#/components/schemas/Timestamp'
      required:
      - id
      - paymentId
      - status
    PaystackInitRequest:
      type: object
      properties:
        orderIds:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
        bookingId:
          $ref: '#/components/schemas/Uuid'
        membershipId:
          $ref: '#/components/schemas/Uuid'
        amount:
          $ref: '#/components/schemas/Money'
        email:
          type: string
          format: email
        callbackUrl:
          type: string
          format: uri
      required:
      - amount
      - email
      description: Provide exactly one of `orderIds`, `bookingId`, `membershipId`.
    PaystackInitResult:
      type: object
      properties:
        paymentId:
          $ref: '#/components/schemas/Uuid'
        reference:
          type: string
        authorizationUrl:
          type: string
          format: uri
        accessCode:
          type: string
      required:
      - paymentId
      - reference
      - authorizationUrl
    ProviderEventAck:
      type: object
      properties:
        received:
          type: boolean
        duplicate:
          type: boolean
      required:
      - received
    CashSession:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        facilityId:
          $ref: '#/components/schemas/Uuid'
        deviceId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        staffId:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
          enum:
          - OPEN
          - CLOSED
        openingFloat:
          $ref: '#/components/schemas/Money'
        expectedCash:
          $ref: '#/components/schemas/Money'
        countedCash:
          oneOf:
          - $ref: '#/components/schemas/Money'
          - type: 'null'
        variance:
          oneOf:
          - $ref: '#/components/schemas/Money'
          - type: 'null'
        openedAt:
          $ref: '#/components/schemas/Timestamp'
        closedAt:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
      required:
      - id
      - facilityId
      - staffId
      - status
      - openingFloat
      - openedAt
    OpenCashSessionRequest:
      type: object
      properties:
        facilityId:
          $ref: '#/components/schemas/Uuid'
        openingFloat:
          $ref: '#/components/schemas/Money'
      required:
      - facilityId
      - openingFloat
    CloseCashSessionRequest:
      type: object
      properties:
        countedCash:
          $ref: '#/components/schemas/Money'
        note:
          type: string
      required:
      - countedCash
    Receipt:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        number:
          type: string
          example: RCP-20260923-000045
        facilityId:
          $ref: '#/components/schemas/Uuid'
        facilityName:
          type: string
        siteName:
          type: string
        siteAddress:
          type: string
        issuedAt:
          $ref: '#/components/schemas/Timestamp'
        cashierName:
          type: string
        orderNumbers:
          type: array
          items:
            type: string
        tableLabel:
          type:
          - string
          - 'null'
        lines:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              quantity:
                type: integer
              unitPrice:
                $ref: '#/components/schemas/Money'
              lineTotal:
                $ref: '#/components/schemas/Money'
            required:
            - name
            - quantity
            - unitPrice
            - lineTotal
        subtotal:
          $ref: '#/components/schemas/Money'
        discountTotal:
          $ref: '#/components/schemas/Money'
        taxTotal:
          $ref: '#/components/schemas/Money'
        total:
          $ref: '#/components/schemas/Money'
        currency:
          type: string
          enum:
          - NGN
        tenders:
          type: array
          items:
            type: object
            properties:
              tenderType:
                $ref: '#/components/schemas/TenderType'
              amount:
                $ref: '#/components/schemas/Money'
              reference:
                type:
                - string
                - 'null'
            required:
            - tenderType
            - amount
        changeGiven:
          $ref: '#/components/schemas/Money'
        vatNumber:
          type:
          - string
          - 'null'
        qrPayload:
          type:
          - string
          - 'null'
          description: Entitlement QR token(s) to print on the receipt, when tickets were issued.
        reprintCount:
          type: integer
        footer:
          type: string
      required:
      - id
      - number
      - facilityName
      - issuedAt
      - lines
      - subtotal
      - taxTotal
      - total
      - currency
      - tenders
      description: Render-ready payload for ESC/POS or PDF printing. Clients do not compute totals.
    KdsStation:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        facilityId:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
          example: Kitchen Pass
        kind:
          type: string
          enum:
          - KITCHEN
          - BAR
          - DISPENSE
        active:
          type: boolean
      required:
      - id
      - facilityId
      - name
      - kind
    PrepTicketStatus:
      type: string
      enum:
      - NEW
      - ACCEPTED
      - IN_PROGRESS
      - READY
      - DISPENSED
      - CANCELLED
    PrepTicketItem:
      type: object
      properties:
        orderLineId:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        quantity:
          type: integer
        notes:
          type:
          - string
          - 'null'
        status:
          $ref: '#/components/schemas/PrepTicketStatus'
      required:
      - orderLineId
      - name
      - quantity
    PrepTicket:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        number:
          type: string
          example: K-042
        stationId:
          $ref: '#/components/schemas/Uuid'
        orderId:
          $ref: '#/components/schemas/Uuid'
        orderNumber:
          type: string
        facilityId:
          $ref: '#/components/schemas/Uuid'
        tableLabel:
          type:
          - string
          - 'null'
        status:
          $ref: '#/components/schemas/PrepTicketStatus'
        items:
          type: array
          items:
            $ref: '#/components/schemas/PrepTicketItem'
        createdAt:
          $ref: '#/components/schemas/Timestamp'
        acceptedAt:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
        readyAt:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
        waiterStaffId:
          $ref: '#/components/schemas/Uuid'
        waiterName:
          type: string
        rowVersion:
          type: integer
      required:
      - id
      - number
      - stationId
      - orderId
      - status
      - items
      - createdAt
      - rowVersion
    TransitionRequest:
      type: object
      properties:
        to:
          type: string
          enum:
          - ACCEPTED
          - IN_PROGRESS
          - READY
          - DISPENSED
        note:
          type: string
      required:
      - to
    InventoryItem:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        sku:
          type: string
        name:
          type: string
        unit:
          type: string
          example: bottle
        category:
          type: string
        reorderLevel:
          type: string
          description: Decimal string.
        active:
          type: boolean
        rowVersion:
          type: integer
      required:
      - id
      - sku
      - name
      - unit
    StockLocation:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        facilityId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        name:
          type: string
          example: Main Store
        kind:
          type: string
          enum:
          - MAIN_STORE
          - FACILITY_STORE
          - BAR
          - KITCHEN
          - WASTE
      required:
      - id
      - name
      - kind
    StockBalance:
      type: object
      properties:
        itemId:
          $ref: '#/components/schemas/Uuid'
        itemName:
          type: string
        locationId:
          $ref: '#/components/schemas/Uuid'
        quantity:
          type: string
          description: Decimal string (up to 4 dp).
        unit:
          type: string
        updatedAt:
          $ref: '#/components/schemas/Timestamp'
      required:
      - itemId
      - locationId
      - quantity
    MovementLine:
      type: object
      properties:
        itemId:
          $ref: '#/components/schemas/Uuid'
        quantity:
          type: string
          description: Positive decimal string.
        unitCost:
          $ref: '#/components/schemas/Money'
      required:
      - itemId
      - quantity
    PurchaseReceiptRequest:
      type: object
      properties:
        locationId:
          $ref: '#/components/schemas/Uuid'
        supplierName:
          type: string
        supplierInvoice:
          type: string
        lines:
          type: array
          items:
            $ref: '#/components/schemas/MovementLine'
      required:
      - locationId
      - lines
    TransferRequest:
      type: object
      properties:
        fromLocationId:
          $ref: '#/components/schemas/Uuid'
        toLocationId:
          $ref: '#/components/schemas/Uuid'
        lines:
          type: array
          items:
            $ref: '#/components/schemas/MovementLine'
        note:
          type: string
      required:
      - fromLocationId
      - toLocationId
      - lines
    AdjustmentInvRequest:
      type: object
      properties:
        locationId:
          $ref: '#/components/schemas/Uuid'
        itemId:
          $ref: '#/components/schemas/Uuid'
        quantityDelta:
          type: string
          description: Signed decimal string.
        reason:
          type: string
          enum:
          - DAMAGE
          - THEFT
          - CORRECTION
          - EXPIRY
          - OTHER
        note:
          type: string
          minLength: 3
      required:
      - locationId
      - itemId
      - quantityDelta
      - reason
      - note
    WastageRequest:
      type: object
      properties:
        locationId:
          $ref: '#/components/schemas/Uuid'
        itemId:
          $ref: '#/components/schemas/Uuid'
        quantity:
          type: string
        reason:
          type: string
          enum:
          - SPOILAGE
          - BREAKAGE
          - PREP_WASTE
          - EXPIRY
          - OTHER
        note:
          type: string
      required:
      - locationId
      - itemId
      - quantity
      - reason
    StockMovement:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        kind:
          type: string
          enum:
          - PURCHASE_RECEIPT
          - TRANSFER_OUT
          - TRANSFER_IN
          - ADJUSTMENT
          - WASTAGE
          - CONSUMPTION
          - COUNT_VARIANCE
        documentId:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
          enum:
          - POSTED
          - PENDING_APPROVAL
        approvalId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        lines:
          type: array
          items:
            type: object
            properties:
              itemId:
                $ref: '#/components/schemas/Uuid'
              locationId:
                $ref: '#/components/schemas/Uuid'
              quantityDelta:
                type: string
              unitCost:
                oneOf:
                - $ref: '#/components/schemas/Money'
                - type: 'null'
            required:
            - itemId
            - locationId
            - quantityDelta
        createdAt:
          $ref: '#/components/schemas/Timestamp'
      required:
      - id
      - kind
      - status
      - lines
    CountRequest:
      type: object
      properties:
        locationId:
          $ref: '#/components/schemas/Uuid'
        lines:
          type: array
          items:
            type: object
            properties:
              itemId:
                $ref: '#/components/schemas/Uuid'
              countedQuantity:
                type: string
            required:
            - itemId
            - countedQuantity
        note:
          type: string
      required:
      - locationId
      - lines
    StockCount:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        locationId:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
          enum:
          - DRAFT
          - POSTED
        lines:
          type: array
          items:
            type: object
            properties:
              itemId:
                $ref: '#/components/schemas/Uuid'
              expectedQuantity:
                type: string
              countedQuantity:
                type: string
              variance:
                type: string
            required:
            - itemId
            - countedQuantity
        createdAt:
          $ref: '#/components/schemas/Timestamp'
        postedAt:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
      required:
      - id
      - locationId
      - status
      - lines
    BookableResource:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        facilityId:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
          example: Lawn Tennis Court 1
        mode:
          type: string
          enum:
          - WHOLE_RESOURCE
          - INDIVIDUAL_CAPACITY
          - TIME_SLOT
        capacity:
          type: integer
        slotMinutes:
          type: integer
        productId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        price:
          $ref: '#/components/schemas/Money'
        active:
          type: boolean
      required:
      - id
      - facilityId
      - name
      - mode
      - slotMinutes
      - price
    Slot:
      type: object
      properties:
        start:
          $ref: '#/components/schemas/Timestamp'
        end:
          $ref: '#/components/schemas/Timestamp'
        available:
          type: boolean
        remainingCapacity:
          type: integer
        price:
          $ref: '#/components/schemas/Money'
      required:
      - start
      - end
      - available
    Availability:
      type: object
      properties:
        resourceId:
          $ref: '#/components/schemas/Uuid'
        from:
          $ref: '#/components/schemas/Timestamp'
        to:
          $ref: '#/components/schemas/Timestamp'
        slots:
          type: array
          items:
            $ref: '#/components/schemas/Slot'
      required:
      - resourceId
      - slots
    HoldRequest:
      type: object
      properties:
        resourceId:
          $ref: '#/components/schemas/Uuid'
        start:
          $ref: '#/components/schemas/Timestamp'
        end:
          $ref: '#/components/schemas/Timestamp'
        quantity:
          type: integer
          minimum: 1
        customer:
          $ref: '#/components/schemas/CustomerInput'
      required:
      - resourceId
      - start
      - end
    CustomerInput:
      type: object
      properties:
        name:
          type: string
        phone:
          type: string
        email:
          type: string
          format: email
        membershipId:
          $ref: '#/components/schemas/Uuid'
      required:
      - name
    BookingStatus:
      type: string
      enum:
      - HELD
      - PENDING_PAYMENT
      - CONFIRMED
      - RESCHEDULED
      - COMPLETED
      - CANCELLED
      - EXPIRED
    Booking:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        number:
          type: string
          example: BK-20260923-0007
        resourceId:
          $ref: '#/components/schemas/Uuid'
        resourceName:
          type: string
        facilityId:
          $ref: '#/components/schemas/Uuid'
        start:
          $ref: '#/components/schemas/Timestamp'
        end:
          $ref: '#/components/schemas/Timestamp'
        quantity:
          type: integer
        status:
          $ref: '#/components/schemas/BookingStatus'
        holdExpiresAt:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
        customer:
          $ref: '#/components/schemas/CustomerInput'
        total:
          $ref: '#/components/schemas/Money'
        amountPaid:
          $ref: '#/components/schemas/Money'
        orderId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        entitlementId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        source:
          type: string
          enum:
          - STAFF
          - ONLINE
        rowVersion:
          type: integer
        createdAt:
          $ref: '#/components/schemas/Timestamp'
      required:
      - id
      - number
      - resourceId
      - start
      - end
      - status
      - total
      - amountPaid
      - rowVersion
    ConfirmBookingRequest:
      type: object
      properties:
        tenders:
          type: array
          items:
            $ref: '#/components/schemas/TenderInput'
        cashSessionId:
          $ref: '#/components/schemas/Uuid'
        paystackReference:
          type: string
          description: Set instead of `tenders` when paid online via Paystack.
      description: Confirms a HELD booking by capturing payment (or referencing a verified Paystack payment). Issues the entitlement
        in the same transaction.
    CancelBookingRequest:
      type: object
      properties:
        reason:
          type: string
      required:
      - reason
    RescheduleBookingRequest:
      type: object
      properties:
        start:
          $ref: '#/components/schemas/Timestamp'
        end:
          $ref: '#/components/schemas/Timestamp'
        reason:
          type: string
      required:
      - start
      - end
    RedeemResultCode:
      type: string
      enum:
      - VALID
      - USED
      - EXPIRED
      - WRONG_FACILITY
      - NOT_YET_VALID
      - CANCELLED
    EntitlementItem:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        kind:
          type: string
          enum:
          - ACCESS
          - RENTAL
          - ITEM
        name:
          type: string
        facilityId:
          $ref: '#/components/schemas/Uuid'
        quantity:
          type: integer
        quantityRedeemed:
          type: integer
        validationMode:
          type: string
          enum:
          - NONE
          - SINGLE_USE
          - MULTIPLE_ENTRY
          - TIME_LIMITED
          - ENTRY_EXIT
          - STAFF_APPROVAL
        validFrom:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
        validUntil:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
        rentalStatus:
          type:
          - string
          - 'null'
          enum:
          - NOT_RELEASED
          - RELEASED
          - RETURNED
          - null
      required:
      - id
      - kind
      - name
      - facilityId
      - quantity
      - quantityRedeemed
      - validationMode
    Entitlement:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        qrToken:
          type: string
          description: Opaque signed token encoded in the QR code.
        status:
          type: string
          enum:
          - ACTIVE
          - CANCELLED
          - EXHAUSTED
          - EXPIRED
        orderId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        bookingId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        holderName:
          type:
          - string
          - 'null'
        items:
          type: array
          items:
            $ref: '#/components/schemas/EntitlementItem'
        issuedAt:
          $ref: '#/components/schemas/Timestamp'
      required:
      - id
      - qrToken
      - status
      - items
      - issuedAt
    IssueEntitlementRequest:
      type: object
      properties:
        orderId:
          $ref: '#/components/schemas/Uuid'
        bookingId:
          $ref: '#/components/schemas/Uuid'
      description: 'Provide `orderId` or `bookingId`. Idempotent per source: issuing again returns the existing entitlement.'
    RedeemRequest:
      type: object
      properties:
        action:
          type: string
          enum:
          - ENTRY
        facilityId:
          $ref: '#/components/schemas/Uuid'
        entitlementItemId:
          $ref: '#/components/schemas/Uuid'
        quantity:
          type: integer
          minimum: 1
      required:
      - action
      description: '`facilityId` defaults to the device checkout facility.'
    RedeemResult:
      type: object
      properties:
        result:
          $ref: '#/components/schemas/RedeemResultCode'
        entitlementId:
          $ref: '#/components/schemas/Uuid'
        itemId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        holderName:
          type:
          - string
          - 'null'
        itemName:
          type:
          - string
          - 'null'
        remaining:
          type: integer
        redemptionId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        message:
          type: string
        expectedFacilityId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        validFrom:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
        validUntil:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
        usedAt:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
      required:
      - result
      - entitlementId
      - message
      description: Always HTTP 200 for a scan outcome, including non-VALID results. Only malformed/unknown tokens produce
        404/422.
    ExitRequest:
      type: object
      properties:
        facilityId:
          $ref: '#/components/schemas/Uuid'
        entitlementItemId:
          $ref: '#/components/schemas/Uuid'
    ReleaseRequest:
      type: object
      properties:
        itemIds:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
        note:
          type: string
        depositCollected:
          $ref: '#/components/schemas/Money'
      required:
      - itemIds
    ReturnRequest:
      type: object
      properties:
        itemIds:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
        condition:
          type: string
          enum:
          - OK
          - DAMAGED
          - LOST
        note:
          type: string
        damageCharge:
          $ref: '#/components/schemas/Money'
      required:
      - itemIds
    MembershipPlan:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
          example: Gold Monthly
        durationDays:
          type: integer
        price:
          $ref: '#/components/schemas/Money'
        currency:
          type: string
          enum:
          - NGN
        facilityIds:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
        visitLimit:
          type:
          - integer
          - 'null'
        active:
          type: boolean
      required:
      - id
      - name
      - durationDays
      - price
    PurchaseMembershipRequest:
      type: object
      properties:
        planId:
          $ref: '#/components/schemas/Uuid'
        customer:
          $ref: '#/components/schemas/CustomerInput'
        facilityId:
          $ref: '#/components/schemas/Uuid'
        tenders:
          type: array
          items:
            $ref: '#/components/schemas/TenderInput'
        cashSessionId:
          $ref: '#/components/schemas/Uuid'
        paystackReference:
          type: string
      required:
      - planId
      - customer
    Membership:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        number:
          type: string
        planId:
          $ref: '#/components/schemas/Uuid'
        planName:
          type: string
        holderName:
          type: string
        status:
          type: string
          enum:
          - PENDING_PAYMENT
          - ACTIVE
          - EXPIRED
          - SUSPENDED
          - CANCELLED
        validFrom:
          $ref: '#/components/schemas/Timestamp'
        validUntil:
          $ref: '#/components/schemas/Timestamp'
        visitsUsed:
          type: integer
        visitLimit:
          type:
          - integer
          - 'null'
        qrToken:
          type: string
      required:
      - id
      - number
      - planId
      - holderName
      - status
      - validFrom
      - validUntil
    MembershipValidation:
      type: object
      properties:
        valid:
          type: boolean
        reason:
          type:
          - string
          - 'null'
          enum:
          - EXPIRED
          - SUSPENDED
          - NOT_ACTIVE
          - WRONG_FACILITY
          - VISIT_LIMIT_REACHED
          - NOT_FOUND
          - null
        membership:
          oneOf:
          - $ref: '#/components/schemas/Membership'
          - type: 'null'
      required:
      - valid
    PunchIngest:
      type: object
      properties:
        terminalSerial:
          type: string
          description: ZKTeco device serial number.
        punches:
          type: array
          items:
            type: object
            properties:
              deviceUserId:
                type: string
                description: PIN/enrolled user id on the terminal.
              punchedAt:
                type: string
                description: Terminal-local time as ISO-8601 with offset, or UTC `Z`.
              verifyMode:
                type: string
                example: FINGERPRINT
              direction:
                type: string
                enum:
                - IN
                - OUT
                - UNKNOWN
              rawId:
                type: string
                description: Terminal record id; dedup key with terminalSerial.
            required:
            - deviceUserId
            - punchedAt
            - rawId
      required:
      - terminalSerial
      - punches
      description: Called by the ZKTeco push bridge/agent, authenticated with an ATTENDANCE_TERMINAL device token (no staff
        session).
    PunchIngestResult:
      type: object
      properties:
        accepted:
          type: integer
        duplicates:
          type: integer
        unmapped:
          type: integer
          description: Punches whose deviceUserId is not mapped to a staff member.
      required:
      - accepted
      - duplicates
      - unmapped
    AttendanceRecord:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        staffId:
          $ref: '#/components/schemas/Uuid'
        staffName:
          type: string
        workDate:
          type: string
          format: date
        clockIn:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
        clockOut:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
        minutesWorked:
          type:
          - integer
          - 'null'
        source:
          type: string
          enum:
          - BIOMETRIC
          - MANUAL_CORRECTION
        status:
          type: string
          enum:
          - OPEN
          - CLOSED
          - NEEDS_REVIEW
      required:
      - id
      - staffId
      - workDate
      - status
    AuditEntry:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        seq:
          type: integer
        occurredAt:
          $ref: '#/components/schemas/Timestamp'
        actorStaffId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        actorName:
          type:
          - string
          - 'null'
        deviceId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        action:
          type: string
          example: order.void.approve
        entityType:
          type: string
        entityId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        facilityId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        before:
          type:
          - object
          - 'null'
          additionalProperties: true
        after:
          type:
          - object
          - 'null'
          additionalProperties: true
        reason:
          type:
          - string
          - 'null'
        prevHash:
          type: string
        hash:
          type: string
        correlationId:
          type:
          - string
          - 'null'
      required:
      - id
      - seq
      - occurredAt
      - action
      - entityType
      - hash
    FacilityDailySummary:
      type: object
      properties:
        facilityId:
          $ref: '#/components/schemas/Uuid'
        date:
          type: string
          format: date
        orders:
          type: integer
        grossSales:
          $ref: '#/components/schemas/Money'
        discounts:
          $ref: '#/components/schemas/Money'
        tax:
          $ref: '#/components/schemas/Money'
        netSales:
          $ref: '#/components/schemas/Money'
        refunds:
          $ref: '#/components/schemas/Money'
        voids:
          type: object
          properties:
            count:
              type: integer
            amount:
              $ref: '#/components/schemas/Money'
          required:
          - count
          - amount
        byTender:
          type: array
          items:
            type: object
            properties:
              tenderType:
                $ref: '#/components/schemas/TenderType'
              amount:
                $ref: '#/components/schemas/Money'
              count:
                type: integer
            required:
            - tenderType
            - amount
        topProducts:
          type: array
          items:
            type: object
            properties:
              productId:
                $ref: '#/components/schemas/Uuid'
              name:
                type: string
              quantity:
                type: integer
              revenue:
                $ref: '#/components/schemas/Money'
            required:
            - productId
            - name
            - quantity
            - revenue
        ticketsRedeemed:
          type: integer
        currency:
          type: string
          enum:
          - NGN
      required:
      - facilityId
      - date
      - orders
      - grossSales
      - netSales
    CashierShiftReport:
      type: object
      properties:
        shiftId:
          $ref: '#/components/schemas/Uuid'
        staffId:
          $ref: '#/components/schemas/Uuid'
        staffName:
          type: string
        facilityId:
          $ref: '#/components/schemas/Uuid'
        openedAt:
          $ref: '#/components/schemas/Timestamp'
        closedAt:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
        openingFloat:
          $ref: '#/components/schemas/Money'
        byTender:
          type: array
          items:
            type: object
            properties:
              tenderType:
                $ref: '#/components/schemas/TenderType'
              amount:
                $ref: '#/components/schemas/Money'
              count:
                type: integer
            required:
            - tenderType
            - amount
        expectedCash:
          $ref: '#/components/schemas/Money'
        countedCash:
          oneOf:
          - $ref: '#/components/schemas/Money'
          - type: 'null'
        variance:
          oneOf:
          - $ref: '#/components/schemas/Money'
          - type: 'null'
        refunds:
          $ref: '#/components/schemas/Money'
        voids:
          type: integer
        paymentIds:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
      required:
      - shiftId
      - staffId
      - facilityId
      - openedAt
      - openingFloat
      - byTender
      - expectedCash
    SyncEvent:
      type: object
      properties:
        eventId:
          $ref: '#/components/schemas/Uuid'
        eventType:
          type: string
          example: OrderCreated
        entityType:
          type: string
        entityId:
          $ref: '#/components/schemas/Uuid'
        entityVersion:
          type: integer
        organizationId:
          $ref: '#/components/schemas/Uuid'
        siteId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        facilityId:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        sourceNode:
          type: string
          enum:
          - local
          - cloud
        occurredAt:
          $ref: '#/components/schemas/Timestamp'
        payload:
          type: object
          additionalProperties: true
      required:
      - eventId
      - eventType
      - entityType
      - entityId
      - entityVersion
      - sourceNode
      - occurredAt
      - payload
      description: See architecture/sync/event-catalogue.md for event types and payloads.
    SyncInboxResult:
      type: object
      properties:
        eventId:
          $ref: '#/components/schemas/Uuid'
        result:
          type: string
          enum:
          - APPLIED
          - DUPLICATE
          - CONFLICT
          - DEFERRED
          - FAILED
        detail:
          type: string
      required:
      - eventId
      - result
    SyncInboxRequest:
      type: object
      properties:
        events:
          type: array
          items:
            $ref: '#/components/schemas/SyncEvent'
      required:
      - events
    SyncInboxResponse:
      type: object
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/SyncInboxResult'
      required:
      - results
    SyncHeartbeatRequest:
      type: object
      properties:
        nodeId:
          $ref: '#/components/schemas/Uuid'
        sentAt:
          $ref: '#/components/schemas/Timestamp'
        appVersion:
          type: string
        outboxDepth:
          type: integer
        oldestUnsyncedAt:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
        lastPulledCursor:
          type:
          - string
          - 'null'
        services:
          type: object
          additionalProperties:
            type: string
            enum:
            - ok
            - degraded
            - down
      required:
      - nodeId
      - sentAt
    SyncHeartbeatResponse:
      type: object
      properties:
        ackAt:
          $ref: '#/components/schemas/Timestamp'
        peerReachable:
          type: boolean
        commandsPending:
          type: integer
        serverVersion:
          type: string
      required:
      - ackAt
    SyncStatus:
      type: object
      properties:
        node:
          type: string
          enum:
          - local
          - cloud
        peerReachable:
          type: boolean
        lastHeartbeatAt:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
        lastPeerHeartbeatAt:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
        outbox:
          type: object
          properties:
            queued:
              type: integer
            failed:
              type: integer
            conflict:
              type: integer
            oldestQueuedAt:
              oneOf:
              - $ref: '#/components/schemas/Timestamp'
              - type: 'null'
          required:
          - queued
          - failed
          - conflict
          - oldestQueuedAt
        inbox:
          type: object
          properties:
            conflict:
              type: integer
            deferred:
              type: integer
          required:
          - conflict
          - deferred
        health:
          type: string
          enum:
          - ONLINE
          - DEGRADED
          - OFFLINE
      required:
      - node
      - peerReachable
      - outbox
      - health
    TaxSetting:
      type: object
      properties:
        vatEnabled:
          type: boolean
        vatRatePercent:
          type: string
          example: '7.5'
        pricesTaxInclusive:
          type: boolean
        vatNumber:
          type:
          - string
          - 'null'
        rowVersion:
          type: integer
      required:
      - vatEnabled
      - vatRatePercent
      - pricesTaxInclusive
    UpdateTaxSettingRequest:
      type: object
      properties:
        vatEnabled:
          type: boolean
        vatRatePercent:
          type: string
        pricesTaxInclusive:
          type: boolean
        vatNumber:
          type: string
    BroadcastAuthRequest:
      type: object
      properties:
        socket_id:
          type: string
          example: '1234.5678'
        channel_name:
          type: string
          example: private-kds.station.0192f6a0-7b1c-7d2e-9a3b-5c4d6e7f8a91
      required:
      - socket_id
      - channel_name
    BroadcastAuthResponse:
      type: object
      properties:
        auth:
          type: string
          example: reverbkey:signature
      required:
      - auth
    OrderPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/OrderSummary'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    ProductPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Product'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    ApprovalPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Approval'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    PaymentPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Payment'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    PrepTicketPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/PrepTicket'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    InventoryItemPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/InventoryItem'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    StockBalancePage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/StockBalance'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    BookingPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Booking'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    AttendanceRecordPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AttendanceRecord'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    AuditEntryPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AuditEntry'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    DiningTablePage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/DiningTable'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    TabPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Tab'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    StaffMemberPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/StaffMember'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    DevicePage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Device'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    SyncEventPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/SyncEvent'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    MembershipPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Membership'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    CategoryPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Category'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    MembershipPlanPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/MembershipPlan'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    BookableResourcePage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/BookableResource'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    StockLocationPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/StockLocation'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    KdsStationPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/KdsStation'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    RolePage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Role'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    RoleAssignmentPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/RoleAssignment'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    OperatingPointPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/OperatingPoint'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    CashSessionPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/CashSession'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    ProductAvailabilityPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ProductAvailability'
        nextCursor:
          type:
          - string
          - 'null'
          description: Opaque cursor; null when no more pages.
      required:
      - items
      - nextCursor
    DeviceStatusReportAck:
      type: object
      properties:
        serverTime:
          $ref: '#/components/schemas/Timestamp'
        commands:
          type: array
          items:
            $ref: '#/components/schemas/DeviceCommand'
        minClientVersion:
          type: string
      required:
      - serverTime
      - commands
